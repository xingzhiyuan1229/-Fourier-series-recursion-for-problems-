# 论文第 3、4、5 节——所有公式逐一解读与推导

**论文原题**：*Approximate Fourier series recursion for problems involving temporal fractional calculus*  
**作者**：Simon Shaw, John R. Whiteman  
**参考文件**：[论文翻译.md](论文翻译.md)

> **阅读说明**：本文档按照论文小节顺序，对第 3 节、第 4 节和第 5 节中出现的**每一个公式**逐一给出：背景说明、逐步推导、符号解读，以及与前后公式之间的内在联系。篇幅不因内容重复而精简，以保证推导链条的完整性和可独立阅读性。

---

## 第 3 节：Fourier 级数近似（The Fourier series approximation）

### 背景概述

第 3 节的目标是为弱奇异幂律核 \(\phi(t) = \phi_0(t+\bar{t})^\alpha\) 在远历史区段上构造一个光滑的 Fourier 级数"代理"函数 \(\psi\)，从而将远历史积分转化为可以递归更新的 Fourier 级数求和，彻底消除对完整历史记录的依赖。

本节所有公式都围绕一个核心思路展开：

> **将 Volterra 历史积分分段，在远离奇异点的区段上用 Fourier 级数替换原核函数。**

---

### 公式 S3-1：Volterra 历史积分的近/远历史分拆

**原文表述**（第 3 节开头）：

给定 \(T_1 \in (0, T)\) 和某个 \(T_x > T\)（取 \(T_x = T + T_1\)），对 \(t \in [0, T]\)，将 Volterra 积分拆成两段：

\[
\int_0^t \phi(t-s)\,u(s)\,ds
= \int_0^{\min\{T_1,\,t\}} \phi(t-s)\,u(s)\,ds
+ \int_{\min\{T_1,\,t\}}^{t} \phi(t-s)\,u(s)\,ds
\tag{S3-1a}
\]

进而，在第二段（远历史段）将 \(\phi\) 替换为代理函数 \(\psi\)：

\[
= \int_0^{\min\{T_1,\,t\}} \phi(t-s)\,u(s)\,ds
+ \int_{\min\{T_1,\,t\}}^{t} \psi(t-s)\,u(s)\,ds
\tag{S3-1b}
\]

**符号说明**：

| 符号 | 含义 |
|------|------|
| \(T_1\) | 近/远历史分界时间长度（用户选择参数） |
| \(T_x = T + T_1\) | 用于构造偶周期延拓的半周期长度 |
| \(\min\{T_1, t\}\) | 当 \(t < T_1\) 时分界点取 \(t\)，当 \(t \geq T_1\) 时取 \(T_1\) |
| \(\psi\) | 对 \(\phi\) 在 \([T_1, T]\) 上的光滑偶周期延拓代理（见下文） |

**推导与物理含义**：

分拆的动机来自于 \(\phi(t) = \phi_0 t^\alpha\)（\(\bar{t}=0\)）在 \(t=0\) 处的弱奇异性。当 \(t > T_1\) 时，卷积变量 \(t - s > T_1\)，奇异点已被"推远"，核函数的值充分光滑，可以被 Fourier 级数高精度逼近。

- **近历史段** \([0, \min\{T_1, t\}]\)：核 \(\phi\) 可能仍在奇异区域附近，必须用精确积分（乘积矩形/梯形规则）处理；
- **远历史段** \([\min\{T_1, t\}, t]\)：时间差 \(t - s \in [T_1, t]\) 充分大，核远离奇异点，可用光滑代理 \(\psi\) 替换。

当 \(t \leq T_1\) 时，远历史段为空集，整个积分由近历史精确处理。当 \(t > T_1\) 时，分界点固定为 \(T_1\)，远历史段为 \([T_1, t]\)。

**与后续公式的联系**：公式 S3-1b 是第 4 节公式 (13)—(14) 离散化的连续时间基础。当引入时间步长 \(\Delta t\) 后，\(T_1\) 对应 \(N_1\) 步（\(N_1 = T_1/\Delta t\)），近历史由乘积矩形规则处理（公式 12），远历史由 Fourier 代理递归（公式 15—16）处理。

---

### 公式 S3-2：代理函数 \(\psi\) 的构造条件

**原文表述**（第 3 节）：

代理函数 \(\psi\) 属于函数空间 \(C^m_{2T_x\text{-ev-per}}(\mathbb{R})\)，即在 \(\mathbb{R}\) 上 \(m\) 次连续可微、以 \(2T_x\) 为周期的偶函数，且满足：

\[
\psi\big|_{[T_1, T]} = \phi\big|_{[T_1, T]}
\tag{S3-2}
\]

**构造方法**（Hermite 多项式拼接）：

**左端多项式** \(p_L : [0, T_1] \to \mathbb{R}\)，次数 \(2m+1\)（\(m \in \mathbb{N}\) 由用户选择）：

\[
p_L^{(k)}(0) = 0, \quad k = 1, 2, \ldots, m
\tag{S3-2L-1}
\]

\[
p_L^{(k)}(T_1) = \phi^{(k)}(T_1), \quad k = 0, 1, \ldots, m
\tag{S3-2L-2}
\]

其中 \(p_L(0)\) 取在 \(\phi(T_1)\) 和 \(\phi\) 在 \(T_1\) 处切线的 \(t=0\) 截距之间的某个值（确保整体光滑性和非负性）。

**右端多项式** \(p_R : [T, T_x] \to \mathbb{R}\)，次数 \(2m+1\)：

\[
p_R^{(k)}(T) = \phi^{(k)}(T), \quad k = 0, 1, \ldots, m
\tag{S3-2R-1}
\]

\[
p_R^{(k)}(T_x) = 0, \quad k = 1, 2, \ldots, m
\tag{S3-2R-2}
\]

其中 \(p_R(T_x)\) 取在 \(\phi(T)\) 和 \(\phi\) 在 \(T\) 处切线的 \(t=T_x\) 截距之间的某个值。

**拼接与延拓**：

1. 在 \([0, T_x]\) 上拼接：\(\psi(t) = p_L(t)\) for \(t \in [0, T_1]\)，\(\psi(t) = \phi(t)\) for \(t \in [T_1, T]\)，\(\psi(t) = p_R(t)\) for \(t \in [T, T_x]\)；
2. 对 \(t=0\) 做**偶对称反射**：将 \(\psi\) 延拓至 \([-T_x, T_x]\)，令 \(\psi(-t) = \psi(t)\)，得到偶函数；
3. 做 **\(2T_x\) 周期延拓**至全实轴 \(\mathbb{R}\)。

**为什么需要这样构造**：

Hermite 约束条件 S3-2L-1（\(p_L\) 在 \(t=0\) 处各阶导数为零）和 S3-2R-2（\(p_R\) 在 \(t=T_x\) 处各阶导数为零）保证了偶周期延拓后函数在 \(t=0\) 和 \(t=\pm T_x\) 处（即周期边界点）的 \(C^m\) 光滑性。没有这些约束，周期延拓会在端点产生跳跃，导致 Fourier 系数衰减率仅为 \(O(k^{-1})\)（Gibbs 现象）。有了这些约束后，\(\psi \in C^m(\mathbb{R})\)，对应 Fourier 系数以 \(O(k^{-(m+1)})\) 速率衰减，截断误差随 \(m\) 增大而快速减小。

\(C^m\) 拼接条件 S3-2L-2 和 S3-2R-1 保证了 \(\psi\) 与 \(\phi\) 在分界点 \(T_1\) 和 \(T\) 处的高阶光滑对接，使代理函数对核函数 \(\phi\) 的逼近误差尽可能小。

**与公式 S3-1 的联系**：条件 S3-2 即 \(\psi = \phi\) on \([T_1, T]\) 保证了在公式 S3-1b 中用 \(\psi\) 替换 \(\phi\) 不引入误差——近似误差仅来自后续的 Fourier 级数截断（有限项 \(L\)）。

---

### 公式 S3-3：一般复数 Fourier 级数展开

**原文表述**（第 3 节）：

对于满足上述构造条件的偶周期函数 \(\psi\)，其完整复数 Fourier 级数为：

\[
\psi(t) = \sum_{k=-\infty}^{+\infty} c_k\, e^{i\pi k t / T_x}
\qquad \text{其中} \qquad
c_k = \frac{1}{2T_x} \int_{-T_x}^{T_x} \psi(t)\, e^{-i\pi k t / T_x}\, dt
\tag{S3-3}
\]

对所有整数 \(-\infty < k < +\infty\)。

**推导**：

这是标准 \(L^2\) 函数空间上的 Fourier 展开。对以 \(2T_x\) 为周期的函数 \(\psi \in L^2([-T_x, T_x])\)，标准正交基为 \(\left\{e^{i\pi k t/T_x}\right\}_{k \in \mathbb{Z}}\)，该族函数在 \([-T_x, T_x]\) 上的内积为：

\[
\left\langle e^{i\pi j t/T_x},\, e^{i\pi k t/T_x} \right\rangle
= \frac{1}{2T_x}\int_{-T_x}^{T_x} e^{i\pi j t/T_x}\, \overline{e^{i\pi k t/T_x}}\, dt
= \frac{1}{2T_x}\int_{-T_x}^{T_x} e^{i\pi(j-k)t/T_x}\, dt
= \delta_{jk}
\]

因为当 \(j \neq k\) 时积分等于 \(\frac{e^{i\pi(j-k)} - e^{-i\pi(j-k)}}{2i\pi(j-k)/T_x \cdot 2T_x}\)（即整数个完整周期，结果为零）；当 \(j = k\) 时被积函数为 1，积分为 \(2T_x/(2T_x)=1\)。

因此 Fourier 系数公式 \(c_k = \langle \psi, e^{i\pi k t/T_x}\rangle\) 即为公式 S3-3 中的积分公式。展开的收敛性（逐点收敛或 \(L^2\) 收敛）由 \(\psi\) 的光滑性保证：\(\psi \in C^m(\mathbb{R})\) 足以保证 Fourier 级数在 \(C^{m-1}\) 意义下一致收敛。

**与后续公式的联系**：公式 S3-3 是 S3-4 的出发点。正是因为 \(\psi\) 是实偶函数，系数的对称性使得双侧无穷和可以折叠为单侧求和，最终得到公式 S3-4 中更便于计算的余弦级数形式。

---

### 公式 S3-4：利用偶实性化简的余弦 Fourier 级数

**原文表述**（第 3 节）：

由于 \(\psi\) 是实值偶函数，有 \(c_k \in \mathbb{R}\) 且 \(c_{-k} = c_k\)，因此公式 S3-3 化简为：

\[
\psi(t) = \sum_{k=0}^{+\infty} c_k\, e^{i\pi k t / T_x}
\tag{S3-4a}
\]

其中 Fourier 系数的计算公式变为：

\[
c_k = \begin{cases}
\dfrac{2}{T_x} \displaystyle\int_0^{T_x} \psi(t)\, \cos\!\left(\dfrac{\pi k t}{T_x}\right) dt, & k \neq 0 \\[10pt]
\dfrac{1}{T_x} \displaystyle\int_0^{T_x} \psi(t)\, dt, & k = 0
\end{cases}
\tag{S3-4b}
\]

**完整推导**：

**第一步：证明实性** \(c_k \in \mathbb{R}\)。

由于 \(\psi\) 是实函数（\(\psi : \mathbb{R} \to \mathbb{R}\)），复共轭系数满足：

\[
\overline{c_k}
= \overline{\frac{1}{2T_x}\int_{-T_x}^{T_x} \psi(t) e^{-i\pi k t/T_x}\,dt}
= \frac{1}{2T_x}\int_{-T_x}^{T_x} \psi(t) e^{+i\pi k t/T_x}\,dt
= c_{-k}
\]

**第二步：证明偶性** \(c_{-k} = c_k\)。

由于 \(\psi\) 是偶函数（\(\psi(-t) = \psi(t)\)），令 \(s = -t\)：

\[
c_{-k} = \frac{1}{2T_x}\int_{-T_x}^{T_x} \psi(t) e^{+i\pi k t/T_x}\,dt
= \frac{1}{2T_x}\int_{-T_x}^{T_x} \psi(-s) e^{-i\pi k s/T_x}\,(-ds)
= \frac{1}{2T_x}\int_{-T_x}^{T_x} \psi(s) e^{-i\pi k s/T_x}\,ds
= c_k
\]

结合实性和偶性，得 \(c_k = \overline{c_k} = c_{-k}\)，即 \(c_k \in \mathbb{R}\) 且 \(c_{-k} = c_k\)。

**第三步：折叠双侧求和**。

原级数 \(\psi(t) = \sum_{k=-\infty}^{+\infty} c_k e^{i\pi k t/T_x}\) 利用 \(c_{-k}=c_k\) 拆分：

\[
\psi(t)
= c_0 + \sum_{k=1}^{+\infty} c_k e^{i\pi k t/T_x} + \sum_{k=1}^{+\infty} c_{-k} e^{-i\pi k t/T_x}
= c_0 + \sum_{k=1}^{+\infty} c_k \left(e^{i\pi k t/T_x} + e^{-i\pi k t/T_x}\right)
= c_0 + \sum_{k=1}^{+\infty} 2c_k \cos\!\left(\frac{\pi k t}{T_x}\right)
\]

这等价于公式 S3-4a，其中 \(k=0\) 的系数为 \(c_0\)，\(k \geq 1\) 的系数为 \(2c_k\)（与 \(e^{i\pi kt/T_x}\) 对应的实部贡献）。

**第四步：化简系数积分**。

对 \(k \neq 0\)，利用 \(\psi\) 的偶性将积分区间折叠至 \([0, T_x]\)：

\[
c_k = \frac{1}{2T_x}\int_{-T_x}^{T_x} \psi(t) e^{-i\pi k t/T_x}\,dt
= \frac{1}{2T_x}\int_{-T_x}^{T_x} \psi(t) \cos\!\left(\frac{\pi k t}{T_x}\right)dt
\quad (\text{因虚部}\,\sin\,\text{关于偶函数为零})
\]

再利用偶性（偶函数 \(\times\) 偶函数 = 偶函数，在对称区间上积分可折半）：

\[
c_k = \frac{1}{2T_x} \cdot 2\int_0^{T_x} \psi(t) \cos\!\left(\frac{\pi k t}{T_x}\right)dt = \frac{1}{T_x}\int_0^{T_x} \psi(t)\cos\!\left(\frac{\pi k t}{T_x}\right)dt
\]

注意这与公式 S3-4b 中 \(k \neq 0\) 的情形有 **2 倍差异**——原文给出的是 \(c_k = \frac{2}{T_x}\int_0^{T_x}\psi(t)\cos(\pi kt/T_x)dt\)。这是因为公式 S3-4a 的写法中，\(k \geq 1\) 的贡献进入级数时已经包含了正负频率的合并，原文的 \(c_k\) 在代入 S3-4a 时其系数即为 \(c_k\)（而非 \(2c_k\)），因此原文的 S3-4b 实际上定义的是"折叠后的"余弦展开系数，等于上面推导中 \(2c_k^{\text{(复数)}}\)。即：

\[
c_k^{(\text{原文})} = 2c_k^{(\text{复数)}} = \frac{2}{T_x}\int_0^{T_x} \psi(t)\cos\!\left(\frac{\pi k t}{T_x}\right)dt, \quad k \neq 0
\]

\[
c_0^{(\text{原文})} = c_0^{(\text{复数})} = \frac{1}{T_x}\int_0^{T_x} \psi(t)\,dt, \quad k = 0
\]

这样公式 S3-4a 就对应完整的余弦展开，等价地可写成：

\[
\psi(t) = c_0 + \sum_{k=1}^{+\infty} c_k \cos\!\left(\frac{\pi k t}{T_x}\right), \quad c_k \text{ 由 S3-4b 给出}
\]

**为什么这样写**：在第 4 节的数值实现（公式 14—16）中，历史变量 \(\mathcal{H}_k(n)\) 同时带有 \(e^{+i\pi k t_n/T_x}\) 的相位（正频分量），配合公式 S3-4a 的写法，实部和虚部分开存储（`phistarR`、`phistarI`），而利用偶实性只需存储 \(k = -L, \ldots, 0\) 或 \(k = 0, 1, \ldots, L\) 共 \(L+1\) 个值，最终贡献通过 \(\text{Re}(\mathcal{H}_k) \times 2\)（\(k \neq 0\)）恢复。

**与公式 S3-3 的联系**：S3-4 是 S3-3 在实偶函数特例下的化简，省去了对负频分量的冗余存储和计算。该化简在代码中体现为只循环 `j in range(-Nlim, 0)`（即 \(k = -L, \ldots, -1\)）并在最终求和时乘以 2。

---

## 第 4 节：矩形—Fourier 规则的实现（Implementation: the rectangle-Fourier rule）

### 背景概述

第 4 节将第 3 节的连续时间分拆思路完全离散化，给出可直接编程实现的时间推进格式。主要内容包括：

1. **基准方法（公式 12）**：纯乘积矩形规则，\(O(N_t^2)\) 运算，\(O(N_t)\) 存储；
2. **分段引入 Fourier 代理（公式 13—14）**：将近历史和远历史分开处理；
3. **Fourier 历史变量的递推（公式 15）**：每步仅 \(O(L)\) 运算；
4. **完整时间推进方案（公式 16）**：合并近历史和 Fourier 远历史。

---

### 参数设置与符号约定

| 符号 | 代码变量 | 含义 |
|------|----------|------|
| \(\Delta t = T/N_t\) | `dt` | 均匀时间步长 |
| \(t_n = n\Delta t\) | `i*dt` | 第 \(n\) 个时间节点 |
| \(N_1 = T_1/\Delta t \in \mathbb{N}\) | `N1` | 近历史步数（要求为整数） |
| \(T_x = T + T_1\) | `Tx` | Fourier 代理的半周期 |
| \(U_n \approx u(t_n)\) | `uvals[n]` | 数值解 |
| \(\bar{t} \geq 0\) | `tbar` | 核函数奇异性偏移 |
| \(d\) | `denom` | 乘积矩形规则的分母（对角项） |
| \(\mathcal{H}_k(n)\) | `phistarR[k+Nlim]+i*phistarI[k+Nlim]` | 第 \(k\) 阶 Fourier 历史变量 |

---

### 公式 S4-1：乘积矩形规则离散方程（公式 12 的出发点）

**原文表述**（第 4 节）：

乘积矩形规则对 Volterra 方程问题 (10) 的离散化为：对 \(U_n \approx u(t_n)\)，\(n = 1, 2, \ldots\) 依次求 \(U_n\) 满足：

\[
U_n + \sum_{j=1}^{n} U_j \int_{t_{j-1}}^{t_j} \phi(t_n - s)\,ds = f(t_n)
\tag{S4-1}
\]

**推导**：

连续时间方程 (10) 为：

\[
u(t) + \int_0^t \phi(t-s)\,u(s)\,ds = f(t)
\]

对时间区间 \([0, t_n]\) 划分为 \(n\) 个子区间 \([t_0, t_1], [t_1, t_2], \ldots, [t_{n-1}, t_n]\)（其中 \(t_j = j\Delta t\)），对每个子区间 \([t_{j-1}, t_j]\) 上的历史积分应用**乘积矩形近似**（product rectangle rule）：

\[
\int_{t_{j-1}}^{t_j} \phi(t_n - s)\,u(s)\,ds \approx U_j \int_{t_{j-1}}^{t_j} \phi(t_n - s)\,ds
\]

即在每段上将 \(u(s)\) 近似为常数 \(U_j = u(t_j)\)（右端点值），而核函数 \(\phi\) 精确积分。将 \(n\) 段求和即得公式 S4-1。

注意：此处 \(j=n\) 项是"对角"项（\(s \in [t_{n-1}, t_n]\)），对应当前步本身，这使方程 S4-1 变为一个关于 \(U_n\) 的隐式方程。

**"乘积"的含义**：之所以称"乘积矩形规则"（product quadrature），是因为该方法将被积函数 \(\phi(t_n-s)\cdot u(s)\) 的积分拆解为：对光滑部分 \(u(s)\) 用矩形近似，对核函数 \(\phi\) 精确积分，乘以矩形近似权重。这与普通矩形规则对整个被积函数直接取矩形近似不同，乘积规则能精确捕捉核函数的弱奇异性。

**与公式 S4-2 的联系**：将 S4-1 中的 \(\int_{t_{j-1}}^{t_j} \phi(t_n-s)ds\) 精确计算，就得到公式 S4-2 的具体表达式。

---

### 公式 S4-2：幂律核在各区间上的精确积分

**原文表述**（第 4 节）：

对于幂律核 \(\phi(t) = \phi_0(t+\bar{t})^\alpha\)（\(\alpha \neq -1\)），每段 \([t_{j-1}, t_j]\) 上的精确积分为：

\[
\phi_0 \int_{t_{j-1}}^{t_j} (\bar{t} + t_n - s)^\alpha\,ds
= \frac{\phi_0}{\alpha+1}\left[(\bar{t} + t_n - t_{j-1})^{\alpha+1} - (\bar{t} + t_n - t_j)^{\alpha+1}\right]
\tag{S4-2a}
\]

代入 \(t_n = n\Delta t\)、\(t_j = j\Delta t\)、\(t_{j-1} = (j-1)\Delta t\)：

\[
= \frac{\phi_0}{\alpha+1}\left[(\bar{t} + (n-j+1)\Delta t)^{\alpha+1} - (\bar{t} + (n-j)\Delta t)^{\alpha+1}\right]
\tag{S4-2b}
\]

**完整推导**：

令 \(\tau = \bar{t} + t_n - s\)，则 \(d\tau = -ds\)，当 \(s = t_{j-1}\) 时 \(\tau = \bar{t} + t_n - t_{j-1}\)，当 \(s = t_j\) 时 \(\tau = \bar{t} + t_n - t_j\)：

\[
\phi_0 \int_{t_{j-1}}^{t_j} (\bar{t} + t_n - s)^\alpha\,ds
= \phi_0 \int_{\bar{t}+t_n-t_{j-1}}^{\bar{t}+t_n-t_j} \tau^\alpha\cdot(-d\tau)
= \phi_0 \int_{\bar{t}+t_n-t_j}^{\bar{t}+t_n-t_{j-1}} \tau^\alpha\,d\tau
= \phi_0 \left[\frac{\tau^{\alpha+1}}{\alpha+1}\right]_{\bar{t}+t_n-t_j}^{\bar{t}+t_n-t_{j-1}}
\]

注意 \(\alpha \in (-1, 0)\) 时 \(\alpha + 1 \in (0, 1) > 0\)，分母非零，计算有效，得公式 S4-2a。

代入均匀步长 \(t_n - t_{j-1} = (n-j+1)\Delta t\) 和 \(t_n - t_j = (n-j)\Delta t\) 得公式 S4-2b。

**特殊情形** \(\bar{t} = 0\)：

\[
\phi_0 \int_{t_{j-1}}^{t_j} (t_n - s)^\alpha\,ds
= \frac{\phi_0}{\alpha+1}\left[((n-j+1)\Delta t)^{\alpha+1} - ((n-j)\Delta t)^{\alpha+1}\right]
= \frac{\phi_0 (\Delta t)^{\alpha+1}}{\alpha+1}\left[(n-j+1)^{\alpha+1} - (n-j)^{\alpha+1}\right]
\]

这表明权重系数只与差值 \(n-j\) 有关（不依赖具体的 \(n\) 和 \(j\) 而只依赖其差），即卷积方程的 Toeplitz（移位不变）结构。这是采用乘积矩形规则的重要数值特性。

**对角项** \(j=n\)：

\[
\phi_0\int_{t_{n-1}}^{t_n}(\bar{t}+t_n-s)^\alpha\,ds
= \frac{\phi_0}{\alpha+1}\left[(\bar{t}+\Delta t)^{\alpha+1} - \bar{t}^{\alpha+1}\right]
\]

当 \(\bar{t}=0\) 时为 \(\frac{\phi_0}{\alpha+1}(\Delta t)^{\alpha+1}\)，有限值（因 \(\alpha+1>0\)），表明虽然 \(\phi(0) = \infty\)（弱奇异），但在 \([t_{n-1},t_n]\) 上的积分是有限的。

**与公式 12（S4-3）的联系**：将 S4-2b 代入 S4-1，就得到可直接计算的公式 12。

---

### 公式 (12)：乘积矩形规则完整表达式

**原文表述**（第 4 节，论文公式编号 12）：

\[
U_n = d^{-1} f(t_n) - d^{-1} \sum_{j=1}^{n-1} \frac{\phi_0 U_j}{\alpha+1}
\left[(\bar{t}+(n-j+1)\Delta t)^{\alpha+1} - (\bar{t}+(n-j)\Delta t)^{\alpha+1}\right]
\tag{12}
\]

其中分母（对角项贡献）：

\[
d := 1 + \int_{t_{n-1}}^{t_n} \phi(t_n-s)\,ds
= 1 + \frac{\phi_0}{\alpha+1}\left[(\bar{t}+\Delta t)^{\alpha+1} - \bar{t}^{\alpha+1}\right]
\tag{12d}
\]

**完整推导**：

从公式 S4-1 出发：

\[
U_n + \sum_{j=1}^n U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds = f(t_n)
\]

将 \(j=n\) 的对角项从求和中分离：

\[
U_n\left(1 + \int_{t_{n-1}}^{t_n}\phi(t_n-s)\,ds\right)
+ \sum_{j=1}^{n-1} U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds = f(t_n)
\]

令 \(d = 1 + \int_{t_{n-1}}^{t_n}\phi(t_n-s)\,ds\)，解出 \(U_n\)：

\[
U_n = \frac{1}{d}\left(f(t_n) - \sum_{j=1}^{n-1} U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds\right)
\]

代入公式 S4-2b 的精确积分结果，即得公式 (12)。

**注意**：分母 \(d\) 不依赖于 \(n\)（因为 \(\bar{t}\)、\(\alpha\)、\(\Delta t\) 都是固定的），故只需在时间循环外计算一次，开销为 \(O(1)\)。

**运算复杂度分析**：公式 (12) 中的求和包含 \(n-1\) 项（历史步 \(j = 1, \ldots, n-1\)），因此第 \(n\) 步的运算量为 \(O(n)\)。从 \(n=1\) 到 \(N_t\) 的总运算量为 \(\sum_{n=1}^{N_t}(n-1) = O(N_t^2)\)。存储量为 \(O(N_t)\)（需保存所有历史 \(U_1, \ldots, U_{n-1}\)）。这正是本文要改善的"朴素方法"。

**代码实现**（`fouvol.py`，solver=1）：

```python
# 分母只计算一次（与 n 无关）
denom = 1.0 + varphi0/(alpha+1)*( (dt+tbar)**(alpha+1) - tbar**(alpha+1) )

for i in range(1, Nt+1):   # n = 1, 2, ..., Nt
    hist = sum([
        varphi0/(alpha+1) * (
            ((i-j+1)*dt + tbar)**(alpha+1) - ((i-j)*dt + tbar)**(alpha+1)
        ) * uvals[j]
        for j in range(1, i)   # j = 1, ..., n-1
    ])
    uvals[i] = ( ft(i*dt, varphi0, alpha) - hist ) / denom
```

---

### 公式 (13)：引入 Fourier 代理的方程重写

**原文表述**（第 4 节，论文公式编号 13）：

当 \(t_n > T_1\)（即 \(n > N_1\)）时，方程 S4-1 可以改写为：

\[
\left(1 + \int_{t_{n-1}}^{t_n}\phi(t_n-s)\,ds\right) U_n
= f(t_n) - \sum_{j=1}^{n-1} U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds
\tag{13}
\]

**推导**：

这只是方程 S4-1 的代数等价形式——将 \(j=n\) 的对角项移至左侧（乘以 \(U_n\)）、将其余历史项保留在右侧。等同于公式 (12) 的分子/分母写法，但此处还未代入精确积分结果，保持积分形式，便于接下来分拆近历史和远历史。

**为什么这步单独列出**：公式 (13) 是从纯朴素矩形法到 Fourier 代理方法的过渡。它是公式 (14) 中历史分拆的直接出发点，显示出历史求和 \(\sum_{j=1}^{n-1}(\ldots)\) 是唯一需要修改的部分（分母 \(d\) 不变）。

---

### 公式 (14)：引入 Fourier 代理后的完整分拆

**原文表述**（第 4 节，论文公式编号 14）：

将历史求和分为近历史（\(j = n-N_1+1, \ldots, n-1\)，用精确积分）和远历史（\(j = 1, \ldots, n-N_1\)，用 Fourier 代理），并将代理的 Fourier 级数代入：

\[
\left(1 + \int_{t_{n-1}}^{t_n}\phi(t_n-s)\,ds\right) U_n
\approx f(t_n)
- \sum_{j=n-N_1+1}^{n-1} U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds
- \sum_{k=0}^{L} c_k \sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_n-s)/T_x}\,ds
\tag{14}
\]

**逐步推导**：

**第一步**：将历史求和拆成近历史和远历史两段：

\[
\sum_{j=1}^{n-1} U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds
= \underbrace{\sum_{j=n-N_1+1}^{n-1}(\ldots)}_{\text{近历史，}t_n-s\in[T_1,(N_1-1)\Delta t]}
+ \underbrace{\sum_{j=1}^{n-N_1}(\ldots)}_{\text{远历史，}t_n-s\geq N_1\Delta t=T_1}
\]

**第二步**：在远历史段（\(j = 1, \ldots, n-N_1\)），时间差 \(t_n - s \geq t_n - t_{n-N_1} = N_1\Delta t = T_1\)，核函数处于代理函数 \(\psi\) 有效范围（\([T_1, T]\)），故将 \(\phi\) 替换为 \(\psi\)（无误差，因 \(\psi = \phi\) on \([T_1, T]\)）：

\[
\sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds
= \sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j}\psi(t_n-s)\,ds
\]

**第三步**：将代理函数 \(\psi\) 展开为 Fourier 级数（公式 S3-4a），截断为 \(L\) 阶：

\[
\sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j}\psi(t_n-s)\,ds
\approx \sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j} \sum_{k=0}^{L} c_k e^{i\pi k(t_n-s)/T_x}\,ds
= \sum_{k=0}^{L} c_k \sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_n-s)/T_x}\,ds
\]

最后一步是交换有限求和与积分的顺序（合法因均为有限量）。注意截断 \(L < \infty\) 引入了近似误差（\(\approx\) 符号的来源），误差大小由 Fourier 系数 \(c_k\) 的衰减速率控制。

**近历史段中 \(\phi\) 的精确积分**仍用公式 S4-2b 计算；远历史段中的指数积分见公式 S4-4（定义 \(\mathcal{H}_k\) 时详细给出）。

**与公式 12 的区别**：公式 (12) 的历史求和包含 \(O(n)\) 项（\(j=1,\ldots,n-1\)），每步 \(O(n)\) 运算；公式 (14) 的近历史段只有 \(O(N_1)\) 项（固定步数！），远历史通过 Fourier 变量 \(\mathcal{H}_k\) 递推（见公式 15），每步 \(O(L)\) 运算，总开销降为 \(O(N_t(N_1+L))\)。

---

### 公式 S4-3：Fourier 历史变量 \(\mathcal{H}_k(n)\) 的定义

**原文表述**（第 4 节）：

定义第 \(k\) 阶 Fourier 历史变量：

\[
\mathcal{H}_k(n) := c_k \sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_n-s)/T_x}\,ds
\tag{S4-3a}
\]

类似地，前一步的 Fourier 历史变量为：

\[
\mathcal{H}_k(n-1) := c_k \sum_{j=1}^{(n-1)-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_{n-1}-s)/T_x}\,ds
\tag{S4-3b}
\]

**物理含义**：

\(\mathcal{H}_k(n)\) 是当前时间步 \(t_n\) 所"感受"到的来自远历史（\(j=1,\ldots,n-N_1\)）的 Fourier 第 \(k\) 模贡献，单位与 \(c_k \cdot U \cdot \Delta t\) 相同。它是一个复数量（因为 \(e^{i\pi k(t_n-s)/T_x}\) 是复数），在代码中分实虚部存储。

注意 \(\mathcal{H}_k(n-1)\) 和 \(\mathcal{H}_k(n)\) 在求和上限上差 1（分别到 \(n-1-N_1\) 和 \(n-N_1\)），且指数中的时间点分别为 \(t_{n-1}\) 和 \(t_n\)。正是这两个差异导致了简洁的递推关系（公式 15）。

**关系到公式 (14)**：公式 (14) 右边第三项就是 \(\sum_{k=0}^L \mathcal{H}_k(n)\)。

---

### 公式 (15)：Fourier 历史变量的递推更新（核心递推公式）

**原文表述**（第 4 节，论文公式编号 15）：

\[
\mathcal{H}_k(n)
= e^{i\pi k\Delta t/T_x}\,\mathcal{H}_k(n-1)
+ c_k\, U_{n-N_1}\int_{t_{n-N_1-1}}^{t_{n-N_1}} e^{i\pi k(t_n-s)/T_x}\,ds
\tag{15}
\]

**完整推导**：

从公式 S4-3a 出发：

\[
\mathcal{H}_k(n)
= c_k \sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_n-s)/T_x}\,ds
\]

将求和分为最新添加的项（\(j = n-N_1\)）和其余项（\(j = 1, \ldots, n-1-N_1\)）：

\[
= c_k U_{n-N_1} \int_{t_{n-N_1-1}}^{t_{n-N_1}} e^{i\pi k(t_n-s)/T_x}\,ds
+ c_k \sum_{j=1}^{n-1-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_n-s)/T_x}\,ds
\]

对第二个求和，将指数中的 \(t_n\) 写成 \(t_{n-1} + \Delta t\)：

\[
e^{i\pi k(t_n-s)/T_x} = e^{i\pi k(t_{n-1}+\Delta t-s)/T_x} = e^{i\pi k\Delta t/T_x} \cdot e^{i\pi k(t_{n-1}-s)/T_x}
\]

代入：

\[
c_k \sum_{j=1}^{n-1-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_n-s)/T_x}\,ds
= e^{i\pi k\Delta t/T_x} \cdot c_k \sum_{j=1}^{n-1-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_{n-1}-s)/T_x}\,ds
= e^{i\pi k\Delta t/T_x}\,\mathcal{H}_k(n-1)
\]

合并两项即得公式 (15)。

**几何直观**：

公式 (15) 的第一项 \(e^{i\pi k\Delta t/T_x}\,\mathcal{H}_k(n-1)\) 将前一步的历史积分量在复平面上旋转一个角度 \(\theta_k = \pi k\Delta t/T_x\)（相当于时钟向前推进一步），等同于 Prony 指数核中的衰减因子 \(e^{-\Delta t/\tau_q}\)（只是这里变成了纯旋转而非实数衰减）。第二项则加入本步新进入远历史范围的节点 \(U_{n-N_1}\) 的贡献。

**为什么这是关键创新**：更新公式 (15) 只需 \(O(1)\) 次运算（给定 \(k\)），对所有 \(k = 0, 1, \ldots, L\) 共 \(O(L)\) 次运算，**与 \(n\)（已经历的总步数）无关**。这意味着远历史的计算开销是**常数**，彻底打破了朴素方法 \(O(n)\) 递增的瓶颈。

**第 \(k=0\) 的特殊情形**：

当 \(k=0\) 时，\(e^{i\pi\cdot 0\cdot\Delta t/T_x} = 1\)（无旋转），指数积分退化为：

\[
\int_{t_{n-N_1-1}}^{t_{n-N_1}} e^{0}\,ds = \Delta t
\]

因此：

\[
\mathcal{H}_0(n) = \mathcal{H}_0(n-1) + c_0\,U_{n-N_1}\,\Delta t
\tag{15, k=0}
\]

即零频历史变量是对所有 \(j = 1, \ldots, n-N_1\) 的 \(c_0 U_j \Delta t\) 的累积求和（矩形近似的均值积分）。代码中：`phistarR[0+Nlim] += cnR[0+Nlim]*uvals[i-N1]*dt`。

**一般 \(k \neq 0\) 的指数积分**：

公式 (15) 第二项中的指数积分：

\[
\int_{t_{n-N_1-1}}^{t_{n-N_1}} e^{i\pi k(t_n-s)/T_x}\,ds
\]

令 \(\omega = \pi k/T_x\)，\(a = t_n - t_{n-N_1} = N_1\Delta t = T_1\)，\(b = t_n - t_{n-N_1-1} = (N_1+1)\Delta t = T_1 + \Delta t\)：

\[
= \int_a^b e^{i\omega\tau}\,d\tau = \frac{e^{i\omega b} - e^{i\omega a}}{i\omega}
= \frac{e^{i\omega(T_1+\Delta t)} - e^{i\omega T_1}}{i\pi k/T_x}
= \frac{T_x}{i\pi k} e^{i\omega T_1}\left(e^{i\omega\Delta t} - 1\right)
\]

分实虚部：令 \(c_\theta = \cos(\omega\Delta t)\)、\(s_\theta = \sin(\omega\Delta t)\)、\(c_1 = \cos(\omega T_1)\)、\(s_1 = \sin(\omega T_1)\)：

\[
\frac{T_x}{\pi k}\left(c_1 s_\theta - s_1(1-c_\theta)\right) + i\frac{T_x}{\pi k}\left(c_1(1-c_\theta) + s_1 s_\theta\right)
\]

这正是代码中的：

```python
coeff = cnR[j+Nlim] * uvals[i-N1] * Tx / j / math.pi
# 实部更新
phistarR[j+Nlim] += coeff * (cosT1*sinterm - sinT1*(1.0 - costerm))
# 虚部更新
phistarI[j+Nlim] += coeff * (cosT1*(1.0 - costerm) + sinT1*sinterm)
```

**代码实现**（`fouvol.py`，solver=3）：

```python
if i > N1:
    for j in range(-Nlim, 0):         # k = -L, ..., -1 （利用偶实性）
        Rpart = phistarR[j+Nlim]       # Re[H_k(n-1)]
        Ipart = phistarI[j+Nlim]       # Im[H_k(n-1)]
        costerm = math.cos(math.pi*j*dt/Tx)   # Re[e^{iπkΔt/Tx}]
        sinterm = math.sin(math.pi*j*dt/Tx)   # Im[e^{iπkΔt/Tx}]
        # 公式(15)第一项：复数旋转 e^{iπkΔt/Tx} * H_k(n-1)
        phistarR[j+Nlim] = costerm*Rpart - sinterm*Ipart
        phistarI[j+Nlim] = sinterm*Rpart + costerm*Ipart
        # 公式(15)第二项：新步 U_{n-N1} 的贡献
        cosT1 = math.cos(math.pi*j*T1/Tx)
        sinT1 = math.sin(math.pi*j*T1/Tx)
        coeff = cnR[j+Nlim] * uvals[i-N1] * Tx / j / math.pi
        phistarR[j+Nlim] += coeff * (cosT1*sinterm - sinT1*(1.0 - costerm))
        phistarI[j+Nlim] += coeff * (cosT1*(1.0 - costerm) + sinT1*sinterm)
    # k=0 特殊情形
    phistarR[0+Nlim] += cnR[0+Nlim] * uvals[i-N1] * dt
```

---

### 公式 (16)：完整的矩形—Fourier 时间推进方案

**原文表述**（第 4 节，论文公式编号 16）：

\[
U_n = d^{-1} f(t_n)
- d^{-1} \sum_{j=n-N_1+1}^{n-1} U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds
- d^{-1} \sum_{k=0}^{L} \mathcal{H}_k(n)
\tag{16}
\]

其中分母 \(d\) 与公式 (12) 相同（公式 12d），近历史积分用公式 S4-2b 精确计算，远历史由 \(\mathcal{H}_k(n)\) 通过公式 (15) 递推得到。

**推导**：

从公式 (14) 出发，两边除以 \(d = 1 + \int_{t_{n-1}}^{t_n}\phi(t_n-s)\,ds\)：

\[
U_n = d^{-1} f(t_n) - d^{-1} \sum_{j=n-N_1+1}^{n-1} U_j \int_{t_{j-1}}^{t_j}\phi(t_n-s)\,ds - d^{-1} \sum_{k=0}^L \mathcal{H}_k(n)
\]

这里 \(j=n\) 的对角项已移至左边（包含在 \(d\) 中），近历史代入精确积分 S4-2b，远历史用 \(\mathcal{H}_k\) 的定义 S4-3a 代入。

**运算量分析**：

- 近历史求和（\(j = n-N_1+1, \ldots, n-1\)）：\(O(N_1)\) 项，每项计算权重 \(O(1)\)，共 \(O(N_1)\)；
- Fourier 贡献（\(k = 0, \ldots, L\)）：\(O(L)\)，无需循环历史；
- 每步总运算：\(O(N_1 + L)\)（与 \(n\) **无关**）。

整个 \(N_t\) 步的总运算量：\(O(N_t(N_1 + L))\)，对比朴素方法的 \(O(N_t^2)\)，当 \(N_1 = \xi N_t\)（\(\xi \ll 1\)）且 \(L \ll N_t\) 时节省大量计算。

**存储量**：仅需保留近历史的 \(N_1\) 个 \(U\) 值（用于近历史求和）和 \(2L+1\) 个复数历史变量 \(\mathcal{H}_k\)（仅 \(O(L)\)），**完全不需要存储完整的 \(N_t\) 步历史**，存储量从 \(O(N_t)\) 降至 \(O(N_1 + L)\)。

**Fourier 贡献的实部提取**：

公式 (16) 中 \(\sum_{k=0}^L \mathcal{H}_k(n)\) 应为实数（因为 \(U_n\)、\(f(t_n)\)、\(d\) 均为实数，近历史项也为实数）。代理函数 \(\psi\) 的实偶性保证了这一点：

\[
\sum_{k=0}^L \mathcal{H}_k(n) = \text{Re}\left(\mathcal{H}_0(n)\right) + 2\sum_{k=1}^L \text{Re}\left(\mathcal{H}_k(n)\right)
\]

（利用 \(\mathcal{H}_{-k} = \overline{\mathcal{H}_k}\) 和偶实性）。代码实现：

```python
if i > N1:
    hist += phistarR[0+Nlim]                              # k=0 贡献
    hist += 2*sum([phistarR[j+Nlim] for j in range(-Nlim, 0)])  # k=-1,...,-L（乘以2等价于正负频合并）
uvals[i] = ( ft(i*dt, varphi0, alpha) - hist ) / denom
```

**与公式 (12) 的关系**：当 \(N_1 = N_t\)（所有历史都是"近历史"）且不启用 Fourier 代理时，公式 (16) 退化为公式 (12)，即朴素矩形法。两个公式共用同一个分母 \(d\)，Fourier 代理只是对远历史部分的替代处理，不影响近历史精度。

**完整时间推进算法总结**：

1. 预计算 Fourier 系数 \(c_k\)（\(k = 0, 1, \ldots, L\)）和 Hermite 样条系数（仅需一次）；
2. 用公式 (12) 计算 \(U_1, U_2, \ldots, U_{N_1}\)（近历史阶段）；
3. 初始化所有 \(\mathcal{H}_k(N_1) = 0\)（\(k = 0, 1, \ldots, L\)）；
4. 对 \(n = N_1+1, N_1+2, \ldots, N_t\)：
   - 用公式 (15) 更新每个 \(\mathcal{H}_k(n)\)（\(O(L)\) 次运算）；
   - 用公式 (16) 求解 \(U_n\)（\(O(N_1+L)\) 次运算）。

---

## 第 5 节：数值实验（Numerical experiments）

### 背景概述

第 5 节通过具体数值实验验证方法的有效性，并讨论算法参数对精度和计算时间的影响。该节包含的公式和关系式主要涉及：参数设置、\(T_1\) 的代码调整逻辑、精确解形式、误差度量，以及若干展望性的计算量改进公式。

---

### 公式 S5-1：数值实验的参数设置与误差度量

**参数设置**（固定参数）：

\[
\phi_0 = 0.2,\quad \alpha = -0.5,\quad \bar{t} = 0,\quad T = 10
\tag{S5-1a}
\]

\[
T_1 \in \{0.5,\, 0.1,\, 0.05\}
\quad\Leftrightarrow\quad \xi = T_1/T \in \{5\%,\, 1\%,\, 0.5\%\}
\tag{S5-1b}
\]

\[
N_t \in \{2^n : n = 5, 6, \ldots, 16\},\quad L \in \{2^{2n+1} : n = 1, 2, \ldots, 5\}
\tag{S5-1c}
\]

即 \(N_t \in \{32, 64, 128, \ldots, 65536\}\)，\(L \in \{8, 32, 128, 512, 2048\}\)。

**误差定义**：

\[
\text{误差} = |u(T) - U_{N_t}| = |u(10) - U_{N_t}|
\tag{S5-1d}
\]

即终止时刻 \(T=10\) 处的绝对误差（点误差，而非全局 \(L^2\) 或 \(L^\infty\) 误差）。

**为什么用点误差**：点误差最易计算，且对线性问题终点误差通常能代表整体误差水平；同时避免了逐步误差积分的复杂性。

---

### 公式 S5-2：精确解和 \(L^\infty\) 范数

**原文表述**（第 5 节）：

数值实验的精确解取为（来自第 2 节公式 11）：

\[
u(t) = t^{-\alpha}
\tag{S5-2a}
\]

对应的 \(L^\infty\) 范数：

\[
\| u \|_{L^\infty(0,T)} = \max_{t \in [0,10]} t^{-\alpha} = T^{-\alpha} = 10^{0.5} = \sqrt{10} \approx 3.16
\tag{S5-2b}
\]

（注意对 \(\alpha = -0.5\)，\(t^{-\alpha} = t^{0.5}\) 是单调增函数，最大值在 \(t=T=10\) 处取到。）

**从精确解到强迫函数的推导**（复习第 2 节公式 11）：

代入 \(u(t) = t^{-\alpha}\)（\(\alpha=-0.5\)，即 \(u(t)=\sqrt{t}\)）和 \(\phi(t) = \phi_0 t^\alpha = 0.2 t^{-0.5}\) 到方程 (10)：

\[
f(t) = t^{-\alpha} + \phi_0 \int_0^t (t-s)^\alpha s^{-\alpha}\,ds
= t^{0.5} + 0.2 \cdot \underbrace{(t^\alpha * t^{-\alpha})}_{= t\cdot\pi/\sin(\alpha\pi)\text{ 用Laplace}}
= t^{0.5} + 0.2 \cdot t^{-0.5} \cdot \frac{\pi}{\sin(-0.5\pi)}
= t^{0.5} + 0.2\pi t^{0.5}
= t^{0.5}(1 + 0.2\pi)
\]

（利用 \(\alpha\pi = -\pi/2\)，\(\sin(-\pi/2) = -1\)，故 \(\pi/\sin(\alpha\pi) = \pi/(-1) = -\pi\)，实际上需要仔细处理符号；公式 (11) 中给出 \(f(t) = t^{-\alpha}+\phi_0 t^\alpha\cdot\pi/\sin(\alpha\pi)\)，代入后：\(f(t) = t^{0.5} + 0.2t^{-0.5}\cdot(-\pi)\)。从代码 `ft(t,varphi0,alpha)` 可验证：`t**(-alpha) + varphi0*math.pi*(-alpha)*t/math.sin(-math.pi*alpha)`。）

**代码实现**（`fouvol.py`）：

```python
def ft(t, varphi0, alpha):
    # 公式(11)对应的强迫函数 f(t)
    return t**(-alpha) + varphi0 * math.pi * (-alpha) * t / math.sin(-math.pi * alpha)
```

**相对误差**：实验中的参考精度以相对误差表示：

\[
\text{相对误差} = \frac{|u(T) - U_{N_t}|}{\| u \|_{L^\infty(0,T)}} \approx \frac{|u(T) - U_{N_t}|}{3.16}
\tag{S5-2c}
\]

例如论文中提到：朴素矩形法（图3）和 \(m=1\) 代理法（图4）均在 \(N_t=128\)、相对误差约 \(0.1\%\) 时达到类似精度，但代理法需要额外 4 秒（用于 Fourier 系数预计算）。

---

### 公式 S5-3：代码中 \(T_1\) 的精确调整逻辑

**原文表述**（第 5 节实现细节）：

由于代码使用常数时间步长 \(\Delta t = T/N_t\)，而 \(T_1\) 可能不是 \(\Delta t\) 的整数倍，代码中做如下调整：

\[
N_1 = \left\lceil \frac{T_1}{\Delta t} \right\rceil
\tag{S5-3a}
\]

\[
T_1 \leftarrow N_1 \cdot \Delta t \quad (\text{调整后的}\ T_1)
\tag{S5-3b}
\]

\[
T_x = T + T_1 \quad (\text{扩展周期})
\tag{S5-3c}
\]

**含义**：向上取整（\(\lceil\cdot\rceil\)）保证了 \(N_1 \Delta t \geq T_1\)，即近历史段不会比预设的 \(T_1\) 更短（更安全），代理函数始终在核函数充分光滑的范围内使用。调整后的 \(T_1\) 略大于原始设置值，但误差可忽略。

**对 \(T_x\) 和 Fourier 系数的影响**：\(T_x\) 的变化会影响 Fourier 基函数 \(e^{i\pi kt/T_x}\) 的频率，进而影响所有 \(c_k\) 系数，因此当 \(T_1\) 调整后，Fourier 系数需要重新计算（这在代码中通过 `get_FourierCoefficients` 函数完成，该函数使用调整后的 \(T_1\) 和 \(T_x\) 计算系数）。

---

### 公式 S5-4：运算量改进展望——\(N_1'\) 减少方案

**原文表述**（第 5 节，展望方向3）：

论文提出通过更高阶正交规则减少近历史段步数 \(N_1\) 的方案：

利用乘积梯形规则（一阶精度，但步长更大）和乘积矩形规则结合，令梯形规则的步长：

\[
\Delta' t = T/N_1' \sim \sqrt{\Delta t}
\tag{S5-4a}
\]

此时 \(N_1\) 减少到：

\[
N_1' = \sqrt{T_1 N_1}
\tag{S5-4b}
\]

**推导**：

梯形规则在步长 \(\Delta' t\) 下的误差为 \(O((\Delta' t)^2)\)，矩形规则在步长 \(\Delta t\) 下的误差为 \(O(\Delta t)\)；为使两者误差相当，令 \((\Delta' t)^2 = C\Delta t\)，即 \(\Delta' t = C^{1/2}(\Delta t)^{1/2} \sim \sqrt{\Delta t}\)（文献 [9] 的选择）。

若 \(N_1 = \xi N_t\)、\(T_1 = \xi T\)，则：

\[
N_1' = \frac{T_1}{\Delta' t} = \frac{\xi T}{\sqrt{T/N_t}} = \xi\sqrt{T N_t}
\tag{S5-4c}
\]

对比 \(N_1 = \xi N_t\)，新方案的近历史步数 \(N_1'\) 显著减少：

\[
\frac{N_1'}{N_1} = \frac{\xi\sqrt{TN_t}}{\xi N_t} = \sqrt{\frac{T}{N_t}}
\tag{S5-4d}
\]

**数值示例**（论文中给出）：取 \(N_t = 1000\)、\(T = 10\)、\(\xi = 5\%\)：

\[
N_1 = 0.05 \times 1000 = 50, \quad N_1' = 0.05\sqrt{10 \times 1000} = 0.05 \times 100 = 5
\tag{S5-4e}
\]

即近历史步数从 50 步减少到 5 步，近历史的 \(O(N_1^2) = O(2500)\) 操作减少至 \(O((N_1')^2) = O(25)\)，节省 100 倍。

**与公式 (12) 的联系**：S5-4 的改进不改变远历史 Fourier 递推（公式 15—16）的结构，只影响近历史段的正交规则选择和步长。改进后整个算法每步运算量降至 \(O(N_1' + L)\)，总运算量为 \(O(N_t(N_1'+L)) = O(N_t\xi\sqrt{TN_t} + N_t L)\)。

---

### 公式 S5-5：当 \(\bar{t} > 0\) 时消除近历史段的极限情形

**原文表述**（第 5 节，展望方向5）：

如果实验数据拟合给出 \(\bar{t} > 0\)（即核函数 \(\phi(t) = \phi_0(t+\bar{t})^\alpha\) 无奇异），则可令：

\[
T_1 = \bar{t}, \quad T_x = T + 2\bar{t}
\tag{S5-5a}
\]

此时 Fourier 级数代理可覆盖整个区间 \([0, T]\)（因为 \(\phi\) 在 \([0, T]\) 上处处光滑），近历史精确积分段可完全省去：

\[
U_n = d^{-1}f(t_n) - d^{-1}\sum_{k=0}^L \mathcal{H}_k(n)
\tag{S5-5b}
\]

（公式 (16) 中的近历史求和消失。）

**含义**：此极限情形下算法结构与 Prony 级数递推**完全相同**，每步运算量仅为 \(O(L)\)，总运算量 \(O(N_t L)\)，存储量 \(O(L)\)，是最理想的情形。这也说明本文方法是 Prony 递推的自然推广：奇异核通过代理变成等效的"非奇异指数和"处理。

**局限性**：论文指出，当 \(\bar{t} > 0\) 时没有类似公式 (11) 那样的解析精确解可用，因此无法从数值实验中验证误差收敛率，本文未对该情形给出数值结果。

---

## 公式关系总图

以下用流程图形式总结各公式之间的依赖关系：

```
第3节
S3-1b（积分分拆）
    │
    ▼
S3-2（Hermite 构造 ψ，保证 C^m 光滑）
    │
    ▼
S3-3（一般复数 Fourier 展开）
    │  实偶函数化简
    ▼
S3-4（余弦 Fourier 展开，系数 c_k）
    │                       │
    ▼                       ▼
第4节                 c_k 用于 H_k 定义
S4-1（乘积矩形规则）
    │  精确积分 S4-2
    ▼
公式(12)（基准方法，O(N²)）
    │  t>T₁ 时分拆为近/远历史
    ▼
公式(13)→公式(14)（引入 Fourier 代理，≈符号）
    │                       │
    ▼                       ▼
S4-3（H_k(n) 定义）      c_k × 指数积分
    │
    ▼
公式(15)（递推更新，O(L)，核心！）
    │
    ▼
公式(16)（完整时间推进：近历史O(N₁) + Fourier O(L)）

第5节
S5-3（T₁ 代码调整）───→ 影响 T_x、c_k 计算
S5-2（精确解，误差量化）
S5-4（N₁' 改进，O(N₁²) → O(N₁'²)）
S5-5（t̄>0 极限，退化为纯 Prony 形式）
```

**核心传承链**：

\[
\underbrace{\text{S3-4}}_{\text{Fourier系数}} \longrightarrow \underbrace{\text{公式(14)}}_{\text{分拆引入代理}} \longrightarrow \underbrace{\text{公式(15)}}_{\text{递推更新（关键！）}} \longrightarrow \underbrace{\text{公式(16)}}_{\text{完整格式}}
\]

\[
\underbrace{\text{S4-2}}_{\text{精确积分权重}} \longrightarrow \underbrace{\text{公式(12)}}_{\text{基准法}} \longrightarrow \underbrace{\text{公式(16)}}_{\text{近历史部分}}
\]

所有公式共同服务于一个目标：将 \(O(N_t^2)\) 的朴素运算降至 \(O(N_t(N_1+L))\)，同时将存储量从 \(O(N_t)\) 降至 \(O(N_1+L)\)，且保持可接受的数值精度。
