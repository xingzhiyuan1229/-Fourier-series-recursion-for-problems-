# 代码逐行解读

**代码仓库**：[xingzhiyuan1229/-Fourier-series-recursion-for-problems-](https://github.com/xingzhiyuan1229/-Fourier-series-recursion-for-problems-)

本文档按**主函数（fouvol.py）运行顺序**，对所有源文件中的每一行代码给出逐段说明与对应数学公式。文档结构如下：

1. [fouvol.py —— 主求解程序](#fouvol)
2. [fouker.py —— 辅助函数库](#fouker)
3. [plotter.py —— 绘图脚本](#plotter)
4. [compare.py —— 比较脚本](#compare)

格式约定：
- 代码块标注文件名与行号范围，例如 `fouvol.py 第16–17行`
- 块级公式使用 `\[...\]`，行内符号使用 `\(...\)`

---

<a id="fouvol"></a>
## 一、fouvol.py —— 主求解程序

### 1.1 文件头与版权声明

```python
# fouvol.py 第1–14行
#!/usr/bin/python
"""
fouvol.py. See for example, https://github.com/variationalform/fouvol
Copyright (c) 2020, Simon Shaw ...
"""
```

第 1 行：shebang 行，指定脚本由 `/usr/bin/python` 解释执行。  
第 4–14 行：模块文档字符串，说明版权信息和许可证。无数学意义。

---

### 1.2 模块导入与显示后端配置

```python
# fouvol.py 第16–26行
import os, sys, getopt, time, math
import numpy as np
import matplotlib as mpl
if os.environ.get('DISPLAY','') == '':
    print('no display found. Using non-interactive Agg backend')
    mpl.use('Agg')
import matplotlib.pyplot as plt
from fouker import *
```

- 第 16 行：导入标准库（操作系统接口、命令行解析、时间、数学函数）。  
- 第 17 行：导入 NumPy，用于向量/矩阵运算，别名 `np`。  
- 第 21–24 行：若环境变量 `DISPLAY` 为空（例如 Docker 容器中无图形界面），则强制使用非交互式 `Agg` 后端，避免运行时崩溃。  
- 第 25 行：导入 matplotlib 绘图模块。  
- 第 26 行：从 `fouker.py` 导入所有公开名称（`*`），包括 Hermite 插值、Fourier 系数计算等辅助函数。

---

### 1.3 辅助函数：精确解 `ut` 与右端函数 `ft`

```python
# fouvol.py 第29–34行
def ut(t, alpha):
    return t**(-alpha)

def ft(t, varphi0, alpha):
    return t**(-alpha) + varphi0*math.pi*(-alpha)*t/math.sin(-math.pi*alpha)
```

**`ut(t, alpha)`** 返回方程 (10) 的精确解：

\[
u(t) = t^{-\alpha}
\]

其中 \(\alpha \in (-1, 0)\)，故 \(-\alpha \in (0, 1)\)，\(u(t) \to +\infty\) 当 \(t \to 0^+\)。

**`ft(t, varphi0, alpha)`** 返回对应的右端函数 \(f(t)\)，由精确解代入方程 (10) 后得到（详见论文第2节，公式 (11)）：

\[
f(t) = t^{-\alpha} + \phi_0 \int_0^t (t-s)^\alpha s^{-\alpha}\, ds
    = t^{-\alpha} + \phi_0\, t\cdot \frac{\alpha\pi}{\sin(\alpha\pi)}
\]

代码中用 `(-alpha)` 替换 \(-\alpha\)，`math.sin(-math.pi*alpha)` 计算 \(\sin(-\pi\alpha)\)，等价变换后得：

\[
f(t) = t^{-\alpha} + \varphi_0 \cdot \pi \cdot (-\alpha) \cdot t \;/\; \sin(-\pi\alpha)
\]

当 \(\alpha \in (-1,0)\) 时，\(\sin(\pi\alpha) < 0\)，故 \(\sin(-\pi\alpha) = -\sin(\pi\alpha) > 0\)，表达式为正，与 \(u(t) = t^{-\alpha}\) 构成一致。

---

### 1.4 帮助函数 `usage`

```python
# fouvol.py 第37–55行
def usage():
    print("-h   or --help")
    ...
    os.system('date +%Y_%m_%d_%H-%M-%S')
    print(time.strftime("%d/%m/%Y at %H:%M:%S"))
```

纯输出函数，打印命令行参数说明与当前时间。无数学内容。

---

### 1.5 命令行参数解析

```python
# fouvol.py 第62–85行
try:
    opts, args = getopt.getopt(sys.argv[1:], "hv:m:s:L:N:T:1:E:a:X:PFI",
                     ["help","garrulous=","smoothness=","solver=","Nlim=",
                      "Nt=","Time=","T1=","preverr=","alpha=",
                      "gfx=","makeplots","FSerrors","invinterp"])
except getopt.GetoptError as err:
    print(err)
    usage()
    sys.exit(2)
```

`getopt.getopt` 解析 `sys.argv[1:]` 中的命令行参数，将结果存入 `opts`（选项列表）和 `args`（额外参数列表）。若参数不合法则打印错误信息并退出（退出码 2）。无直接数学含义。

---

### 1.6 默认参数定义

```python
# fouvol.py 第87–107行
beingloud   =  0        # 冗长级别
smoothness  =  1        # Hermite 插值连续阶 m
solver      =  1        # 求解器（1=矩形，2=梯形，3=Fourier+矩形，4=Fourier+梯形）
Nlim        =  4        # Fourier 级数求和上限 L
Nt          =  4        # 时间步数 Nt
N1          =  Nt       # 近端求积时间步数 N1
varphi0     =  0.2      # 核函数系数 φ₀
alpha       = -0.4      # 幂律指数 α ∈ (-1,0)
T1          =  0.5      # 近端求积终止时间 T1
T           =  math.pi - T1
Tx          =  T + T1   # 扩展周期 Tx = T + T1
tbar        =  0.0      # 时移参数 t̄
Nvals       =  10000    # 绘图采样点数
gfx         =  0
makeplots   =  0
getFSerrors =  0
FSerror     =  0.0
DDuse       =  1        # 1=使用除差法 Hermite，0=矩阵求解
```

关键数学符号对照：

- \(\varphi_0\) ↔ `varphi0`：幂律核系数
- \(\alpha\) ↔ `alpha`：幂律指数，要求 \(\alpha \in (-1, 0)\)
- \(T\) ↔ `T`：最终时刻，积分范围为 \([0, T]\)
- \(T_1\) ↔ `T1`：近端求积上限，满足 \(T_1 < T\)
- \(T_x = T + T_1\) ↔ `Tx`：Fourier 级数的半周期
- \(L\) ↔ `Nlim`：Fourier 级数截断项数（求和范围 \(-L \le k \le L\)）
- \(N_t\) ↔ `Nt`：时间步数，步长 \(\Delta t = T/N_t\)
- \(\bar{t}\) ↔ `tbar`：核函数移位参数

---

### 1.7 命令行参数处理循环

```python
# fouvol.py 第109–152行
for o, a in opts:
    if o in ("-v","--garrulous"):
        beingloud = int(a)
    elif o in ("-m","--smoothness"):
        smoothness = int(a)
    elif o in ("-s","--solver"):
        solver = int(a)
    elif o in ("-L","--Nlim"):
        Nlim = int(a)
    elif o in ("-N","--Nt"):
        Nt = int(a)
    elif o in ("-T","--Time"):
        T = float(a)
    elif o in ("-1","--T1"):
        T1 = float(a)
    elif o in ("-a","--alpha"):
        alpha = float(a)
    elif o in ("-X","--gfx"):
        gfx = float(a)
    elif o in ("-P","--makeplots"):
        makeplots = 1
    elif o in ("-F","--FSerrors"):
        getFSerrors = 1
    elif o in ("-I","--invinterp"):
        DDuse = 0
    else:
        assert False, "unhandled option"
```

逐项覆盖 1.6 节的默认值，用命令行指定值替换。无直接数学内容，是用户接口层。

---

### 1.8 合理性调整（sanity checks）

```python
# fouvol.py 第155–163行
dt = T / Nt
N1 = int(math.ceil(T1 / dt))
T1 = N1 * dt
Tx = T + T1
```

**时间步长**：

\[
\Delta t = \frac{T}{N_t}
\]

**近端步数**（向上取整，保证 \(N_1 \cdot \Delta t \ge T_1\)）：

\[
N_1 = \left\lceil \frac{T_1}{\Delta t} \right\rceil
\]

**调整后的近端时间**（使 \(T_1\) 严格对齐到网格点）：

\[
T_1 \leftarrow N_1 \cdot \Delta t
\]

**扩展半周期**：

\[
T_x = T + T_1
\]

这 4 个量是整个算法的基础网格参数。

---

### 1.9 Fourier 代理系数计算（solver > 2）

```python
# fouvol.py 第173–183行
if solver > 2:
    old_way = 0
    if old_way == 1:
        cnR, pL, pR = get_FourierCoefficients(...)
    else:
        if DDuse == 0:
            cnR, pL, pR = get_FourierCoefficientsNew(...)
        else:
            cnR, zL, DDmatL, zR, DDmatR = get_FourierCoefficientsNew(...)
```

仅当使用 Fourier 求解器（`solver` 为 3 或 4）时执行此分支。  
调用 `get_FourierCoefficientsNew`（见 [2.14 节](#sec-fouker-getFCN)）计算 Fourier 余弦级数的系数向量 `cnR`，以及左/右 Hermite 插值数据。

Fourier 级数 \(F\psi(t)\) 的系数（论文公式，第3节）：

\[
c_k = \frac{1}{T_x} \int_0^{T_x} \psi(t)\cos\!\left(\frac{\pi k t}{T_x}\right) dt, \quad k = 0, 1, \ldots, L
\]

其中 \(\psi\) 是将幂律核 \(\varphi\) 在 \([0,T_1]\cup[T,T_x]\) 上用 Hermite 多项式拼接后得到的光滑偶周期函数。

---

### 1.10 Fourier 级数误差评估与绘图

```python
# fouvol.py 第184–268行
if getFSerrors:
    get_FSConvergenceResults(...)
    exit(0)

FSerror = get_partialerror(varphi0, alpha, Nvals, T1, T, tbar, cnR, Nlim, Tx)

if makeplots > 0:
    fig = plt.figure(figsize=(8,7))
    plt.rc('text', usetex=True)
    ...
    plot_varphi(...)
    plot_partvarphi(...)
    plot_FS(...)
    plot_DDHermite(...) / plot_basicHermite(...)
    plt.savefig('varphirep.png', ...)
    ...
    # 扩展图（4个周期）
    plot_long_FS(...)
    plt.savefig('varphirepextended.png', ...)
```

`get_partialerror` 计算 Fourier 代理在 \([T_1, T]\) 上的 \(L^\infty\) 误差（见 [2.9 节](#sec-fouker-getpartialerror)）：

\[
\text{FSerror} = \max_{t \in [T_1, T]} \left| F\psi(t) - \varphi(t) \right|
\]

若 `makeplots > 0`，则绘制 \(\varphi(t)\)、Hermite 拼接多项式 \(p_L, p_R\)、及 Fourier 代理 \(F\psi(t)\) 的图形，保存为 PNG/EPS。

---

### 1.11 解向量初始化与 Fourier 历史变量

```python
# fouvol.py 第271–275行
uvals = np.zeros(Nt + 1)
cheat = 0
phistarR = np.zeros(2*Nlim + 1)
phistarI = np.zeros(2*Nlim + 1)
```

- `uvals[n]` \(\approx u(t_n) = u(n\Delta t)\)：数值解向量，长度 \(N_t+1\)，初始值全为 0。  
- `phistarR[k+L]` 和 `phistarI[k+L]`：分别存储复数 Fourier 历史变量

\[
\Phi_k^*(t_n) = c_k \int_0^{t_n - T_1} e^{i\pi k(t_n - s)/T_x} u(s)\, ds
\]

的实部和虚部，供求解器 3、4 的递归更新使用（向量长度为 \(2L+1\)，索引偏移 \(L\)）。

---

### 1.12 求解器 1：乘积矩形规则（Product Rectangle Rule）

```python
# fouvol.py 第278–290行
if solver == 1:
    if tbar > 0:
        denom = 1.0 + varphi0/(alpha+1)*( (dt+tbar)**(alpha+1)-tbar**(alpha+1) )
    else:
        denom = 1.0 + varphi0/(alpha+1)*( dt**(alpha+1) )
    for i in range(1, Nt+1):
        hist = sum([varphi0/(alpha+1)*(((i-j+1)*dt+tbar)**(alpha+1)-((i-j)*dt+tbar)**(alpha+1))
                    *uvals[j] for j in range(1, i)])
        uvals[i] = ( ft(i*dt, varphi0, alpha) - hist ) / denom
        if cheat and i < Nt/4:
            uvals[i] = ut(i*dt, alpha)
```

**分母（当前时刻自相互作用系数）**：

在 \([t_{n-1}, t_n]\) 上用矩形规则将 \(u(s)\) 近似为 \(u(t_n) = U_n\)，核函数精确积分：

\[
\int_{t_{n-1}}^{t_n} \varphi_0 (t_n - s + \bar{t})^\alpha\, ds
= \frac{\varphi_0}{\alpha+1}\Big[ (\Delta t + \bar{t})^{\alpha+1} - \bar{t}^{\alpha+1} \Big]
\]

当 \(\bar{t} = 0\)：

\[
\int_0^{\Delta t} \varphi_0 s^\alpha\, ds = \frac{\varphi_0}{\alpha+1} \Delta t^{\alpha+1}
\]

因此分母：

\[
\text{denom} = 1 + \frac{\varphi_0}{\alpha+1}\Big[(\Delta t + \bar{t})^{\alpha+1} - \bar{t}^{\alpha+1}\Big]
\]

对应方程 \(U_n + \text{denom-系数} \cdot U_n = f(t_n) - \text{hist}\) 中 \(U_n\) 系数的分母。

**历史求和（hist）**：

对 \(j = 1, 2, \ldots, n-1\) 的每个历史时间区间 \([t_{j-1}, t_j]\) 做乘积矩形规则：

\[
\text{hist} = \sum_{j=1}^{n-1} \frac{\varphi_0}{\alpha+1}
\Big[((n-j+1)\Delta t + \bar{t})^{\alpha+1} - ((n-j)\Delta t + \bar{t})^{\alpha+1}\Big] U_j
\]

物理意义：这是对 Volterra 历史积分 \(\int_0^{t_{n-1}} \varphi(t_n - s) u(s)\, ds\) 的数值近似。

**时间步进**：

\[
U_n = \frac{f(t_n) - \text{hist}}{\text{denom}}
\]

这是将 \(u(t_n) + (\varphi * u)(t_n) = f(t_n)\) 的离散形式对 \(U_n\) 求解的结果。

`cheat` 标志（默认为 0）在早期时间步强制使用精确解，主要用于调试。

---

### 1.13 求解器 2：乘积梯形规则（Product Trapezoidal Rule）

```python
# fouvol.py 第293–314行
elif solver == 2:
    if tbar > 0:
        denom = 1.0 + varphi0/(alpha+1)*( (dt+tbar)**(alpha+1)-tbar**(alpha+1) )
    else:
        denom = 1.0 + varphi0/(alpha+1)*( dt**(alpha+1) )
    uvals[1] = ft(dt, varphi0, alpha) / denom
    uvals[0] = uvals[1]
    if tbar > 0:
        denom = 1.0 + 0.5*varphi0/(alpha+1)*( (dt+tbar)**(alpha+1)-tbar**(alpha+1) )
    else:
        denom = 1.0 + 0.5*varphi0/(alpha+1)*( dt**(alpha+1) )
    for i in range(2, Nt+1):
        hist = sum([0.5*varphi0/(alpha+1)*(((i-j+1)*dt+tbar)**(alpha+1)-((i-j)*dt+tbar)**(alpha+1))
                    *(uvals[j]+uvals[j-1]) for j in range(1, i)])
        hist = hist + 0.5*varphi0/(alpha+1)*( (dt+tbar)**(alpha+1)-tbar**(alpha+1) )*uvals[i-1]
        uvals[i] = ( ft(i*dt, varphi0, alpha) - hist ) / denom
```

**第一步（矩形规则启动）**：

\[
U_1 = \frac{f(\Delta t)}{\text{denom}_{\text{rect}}}
\]

其中 \(\text{denom}_{\text{rect}} = 1 + \frac{\varphi_0}{\alpha+1}[(\Delta t+\bar{t})^{\alpha+1}-\bar{t}^{\alpha+1}]\)。  
设 \(U_0 = U_1\)（便于后续历史求和，非真实初始条件）。

**梯形规则分母**：

梯形规则将 \(u(s)\) 在每个子区间 \([t_{j-1}, t_j]\) 上近似为线性插值，结合精确核积分：

\[
\text{denom}_{\text{trap}} = 1 + \frac{1}{2} \cdot \frac{\varphi_0}{\alpha+1}\Big[(\Delta t + \bar{t})^{\alpha+1} - \bar{t}^{\alpha+1}\Big]
\]

即当前时刻贡献项的系数比矩形规则小一半（梯形权重为 \(1/2\)）。

**历史求和（hist）**：

对历史区间采用梯形近似（使用相邻两个时间步的解值的平均）：

\[
\text{hist} = \sum_{j=1}^{n-1} \frac{1}{2} \cdot \frac{\varphi_0}{\alpha+1}
\Big[((n-j+1)\Delta t+\bar{t})^{\alpha+1} - ((n-j)\Delta t+\bar{t})^{\alpha+1}\Big]
(U_j + U_{j-1})
\]

加上最近一步的左端点贡献：

\[
\text{hist} \mathrel{+}= \frac{1}{2} \cdot \frac{\varphi_0}{\alpha+1}
\Big[(\Delta t+\bar{t})^{\alpha+1} - \bar{t}^{\alpha+1}\Big] U_{n-1}
\]

**时间步进**：

\[
U_n = \frac{f(t_n) - \text{hist}}{\text{denom}_{\text{trap}}}
\]

---

### 1.14 求解器 3：Fourier 代理 + 乘积矩形规则

```python
# fouvol.py 第317–353行
elif solver == 3:
    if tbar > 0:
        denom = 1.0 + varphi0/(alpha+1)*( (dt+tbar)**(alpha+1)-tbar**(alpha+1) )
    else:
        denom = 1.0 + varphi0/(alpha+1)*( dt**(alpha+1) )
    for i in range(1, Nt+1):
        # 更新 Fourier 历史变量
        if i > N1:
            for j in range(-Nlim, 0):
                Rpart = phistarR[j+Nlim]; Ipart = phistarI[j+Nlim]
                costerm = math.cos(math.pi*j*dt/Tx)
                sinterm = math.sin(math.pi*j*dt/Tx)
                cosT1   = math.cos(math.pi*j*T1/Tx)
                sinT1   = math.sin(math.pi*j*T1/Tx)
                # 复数衰减
                phistarR[j+Nlim] = costerm*Rpart - sinterm*Ipart
                phistarI[j+Nlim] = sinterm*Rpart + costerm*Ipart
                # 乘积积分改进
                update = cosT1*sinterm - sinT1*(1.0-costerm)
                coeff  = cnR[j+Nlim]*uvals[i-N1]*Tx/j/math.pi
                phistarR[j+Nlim] = phistarR[j+Nlim] + coeff*update
                update = cosT1*(1.0-costerm) + sinT1*sinterm
                phistarI[j+Nlim] = phistarI[j+Nlim] + coeff*update
            # j=0 的特殊情形
            phistarR[0+Nlim] = phistarR[0+Nlim] + cnR[0+Nlim]*uvals[i-N1]*dt
        # 近端求积
        jlo = int(max(1, i+1-N1))
        hist = varphi0/(alpha+1)*sum([(((i-j+1)*dt+tbar)**(alpha+1)-((i-j)*dt+tbar)**(alpha+1))
                *uvals[j] for j in range(jlo, i)])
        # 远端 Fourier 贡献
        if i > N1:
            hist = hist + phistarR[0+Nlim]
            hist = hist + 2*sum([phistarR[j+Nlim] for j in range(-Nlim, 0)])
        uvals[i] = ( ft(i*dt, varphi0, alpha) - hist ) / denom
```

本求解器将 Volterra 历史积分分为两部分：

**近端积分**（\(j_0 \le j \le n-1\)，使用精确核乘积矩形规则）：

\[
\text{hist}_{\text{near}} = \frac{\varphi_0}{\alpha+1}
\sum_{j=j_0}^{n-1}
\Big[((n-j+1)\Delta t + \bar{t})^{\alpha+1} - ((n-j)\Delta t + \bar{t})^{\alpha+1}\Big] U_j
\]

其中 \(j_0 = \max(1,\, n+1-N_1)\)。

**远端历史变量的递归更新**（当 \(n > N_1\)）：

对每个 Fourier 模式 \(k \in \{-L, \ldots, -1\}\)，设

\[
\Phi_k^*(t_n) = c_k \int_0^{t_{n-N_1}} e^{i\pi k(t_n - s)/T_x}\, u(s)\, ds
\]

其实部和虚部分别记为 \(R_k = \text{Re}(\Phi_k^*)\)，\(I_k = \text{Im}(\Phi_k^*)\)。  
复数旋转（"衰减"）因子：

\[
\omega_k = e^{i\pi k \Delta t / T_x} = \cos\!\left(\frac{\pi k \Delta t}{T_x}\right) + i\sin\!\left(\frac{\pi k \Delta t}{T_x}\right)
\]

更新公式：

\[
\begin{pmatrix} R_k \\ I_k \end{pmatrix}_{\text{new}}
= \begin{pmatrix} \cos\theta_k & -\sin\theta_k \\ \sin\theta_k & \cos\theta_k \end{pmatrix}
\begin{pmatrix} R_k \\ I_k \end{pmatrix}_{\text{old}}
\]

其中 \(\theta_k = \pi k \Delta t / T_x\)（代码中的 `costerm`, `sinterm`）。

然后加入新的贡献项（矩形规则对区间 \([t_{n-1-N_1}, t_{n-N_1}]\) 的精确核积分）：

\[
\delta R_k = c_k\, U_{n-N_1}\, \frac{T_x}{\pi k} \left[\cos\!\left(\frac{\pi k T_1}{T_x}\right)\sin\theta_k - \sin\!\left(\frac{\pi k T_1}{T_x}\right)(1 - \cos\theta_k)\right]
\]

\[
\delta I_k = c_k\, U_{n-N_1}\, \frac{T_x}{\pi k} \left[\cos\!\left(\frac{\pi k T_1}{T_x}\right)(1 - \cos\theta_k) + \sin\!\left(\frac{\pi k T_1}{T_x}\right)\sin\theta_k\right]
\]

代码变量对应：
- `costerm` = \(\cos(\pi k \Delta t / T_x)\)，`sinterm` = \(\sin(\pi k \Delta t / T_x)\)  
- `cosT1` = \(\cos(\pi k T_1 / T_x)\)，`sinT1` = \(\sin(\pi k T_1 / T_x)\)  
- `coeff` = \(c_k U_{n-N_1} T_x / (\pi k)\)

**\(k=0\) 的特殊情形**：

\[
\Phi_0^*(t_n) = c_0 \int_0^{t_{n-N_1}} u(s)\, ds \approx \Phi_0^*(t_{n-1}) + c_0\, U_{n-N_1}\, \Delta t
\]

无需旋转，直接累加。

**汇总历史贡献**：

\[
\text{hist} = \text{hist}_{\text{near}} + \Phi_0^* + 2 \sum_{k=-L}^{-1} \text{Re}(\Phi_k^*)
\]

其中因子 2 来自 \(\psi\) 为偶函数时 \(c_k = c_{-k}\)（正负模式合并）：

\[
\sum_{k=-L}^{L} c_k e^{i\pi k \tau / T_x} = c_0 + 2\sum_{k=1}^{L} c_k \cos\!\left(\frac{\pi k \tau}{T_x}\right) = c_0 + 2\sum_{k=-L}^{-1} c_k e^{i\pi k \tau/T_x}
\]

**时间步进**：

\[
U_n = \frac{f(t_n) - \text{hist}}{\text{denom}}
\]

---

### 1.15 求解器 4：Fourier 代理 + 乘积梯形规则

```python
# fouvol.py 第356–402行
elif solver == 4:
    ...
    uvals[1] = ft(dt, varphi0, alpha) / denom   # 矩形规则启动
    uvals[0] = uvals[1]
    ...  # 梯形 denom
    for i in range(2, Nt+1):
        if i > N1:
            for j in range(-Nlim, 0):
                ...
                coeff = cnR[j+Nlim]*(uvals[i-N1]+uvals[i-1-N1])/2.0*Tx/j/math.pi
                ...
            phistarR[0+Nlim] = phistarR[0+Nlim] + cnR[0+Nlim]*(uvals[i-N1]+uvals[i-1-N1])/2.0*dt
        jlo = int(max(1, i+1-N1))
        hist = sum([0.5*varphi0/(alpha+1)*(((i-j+1)*dt+tbar)**(alpha+1)-((i-j)*dt+tbar)**(alpha+1))
               *(uvals[j]+uvals[j-1]) for j in range(jlo, i)])
        hist = hist + 0.5*varphi0/(alpha+1)*((dt+tbar)**(alpha+1)-tbar**(alpha+1))*uvals[i-1]
        if i > N1:
            hist = hist + phistarR[0+Nlim]
            hist = hist + 2*sum([phistarR[j+Nlim] for j in range(-Nlim, 0)])
        uvals[i] = ( ft(i*dt, varphi0, alpha) - hist ) / denom
```

与求解器 3 的结构相同，区别在于：

1. **近端积分**采用梯形规则：

\[
\text{hist}_{\text{near}} = \sum_{j=j_0}^{n-1} \frac{1}{2} \cdot \frac{\varphi_0}{\alpha+1}
\Big[((n-j+1)\Delta t+\bar{t})^{\alpha+1}-((n-j)\Delta t+\bar{t})^{\alpha+1}\Big](U_j + U_{j-1})
\]

并加上最近左端点修正：

\[
\text{hist}_{\text{near}} \mathrel{+}= \frac{1}{2} \cdot \frac{\varphi_0}{\alpha+1}
\Big[(\Delta t+\bar{t})^{\alpha+1}-\bar{t}^{\alpha+1}\Big] U_{n-1}
\]

2. **远端历史更新**中的 `coeff` 使用梯形权重：

\[
\text{coeff}_k = c_k \cdot \frac{U_{n-N_1} + U_{n-1-N_1}}{2} \cdot \frac{T_x}{\pi k}
\]

\[
\delta\Phi_0^* = c_0 \cdot \frac{U_{n-N_1} + U_{n-1-N_1}}{2} \cdot \Delta t
\]

其余公式与求解器 3 完全相同。

---

### 1.16 误差输出

```python
# fouvol.py 第411–412行
error = abs(ut(T, alpha) - uvals[Nt])
print 'FSerror = ', FSerror
print '|u(T)-U| = ', error
```

计算最终时刻 \(T\) 处的数值误差：

\[
\text{error} = |u(T) - U_{N_t}| = |T^{-\alpha} - U_{N_t}|
\]

这是评估数值格式精度的主要指标。

---

### 1.17 绘图函数 `plot_ut` 及最终绘图

```python
# fouvol.py 第416–448行
def plot_ut(uvals, T, Nt, gfx):
    tvals = [float(0.0+i*(T-0.0)/Nt) for i in range(1, Nt+1)]
    ut    = [uvals[i] for i in range(1, Nt+1)]
    plt.rc('text', usetex=True)
    ...
    plt.plot(tvals, ut, "-k", label=r'$U(t)$')
    ...
    plt.savefig('Ut.png', ...)
    plt.savefig('Ut.eps', ...)
    ...

if makeplots > 0:
    plot_ut(uvals, T, Nt, gfx)
```

**`plot_ut` 函数**：

绘制时间向量 \(\{t_i = i\Delta t\}_{i=1}^{N_t}\) 与对应数值解 \(\{U_i\}_{i=1}^{N_t}\) 的折线图，保存为 PNG 和 EPS 格式。  
时间节点：

\[
t_i = i \cdot \frac{T}{N_t}, \quad i = 1, 2, \ldots, N_t
\]

---

<a id="fouker"></a>
## 二、fouker.py —— 辅助函数库

`fouker.py` 由 `fouvol.py` 通过 `from fouker import *` 全量导入。按 `fouvol.py` 的调用顺序逐函数解读。

---

### 2.1 模块导入与显示后端

```python
# fouker.py 第16–28行
from scipy.integrate import quad
import scipy
import os, sys, getopt, time, math
import numpy as np
import matplotlib as mpl
if os.environ.get('DISPLAY','') == '':
    mpl.use('Agg')
import matplotlib.pyplot as plt
import itertools
```

- `scipy.integrate.quad`：自适应数值积分，用于计算 Fourier 系数。  
- 其余同 `fouvol.py`。

---

### 2.2 `plot_varphi` — 绘制完整幂律核

```python
# fouker.py 第31–34行
def plot_varphi(varphi0, alpha, Nvals, T, tbar):
    tvals  = [float(0.0 + i*(T-0.0)/Nvals) for i in range(1, Nvals+1)]
    phivals = [varphi0*(time+tbar)**alpha for time in tvals]
    plt.plot(tvals, phivals, ":g", linewidth=2, markersize=0)
```

在 \((0, T]\) 上绘制幂律核函数（虚线绿色）：

\[
\varphi(t) = \varphi_0 (t + \bar{t})^\alpha
\]

时间采样点：

\[
t_i = \frac{i \cdot T}{N_{\text{vals}}}, \quad i = 1, 2, \ldots, N_{\text{vals}}
\]

---

### 2.3 `plot_partvarphi` — 绘制 \([T_1, T]\) 段幂律核

```python
# fouker.py 第37–40行
def plot_partvarphi(varphi0, alpha, Nvals, T, T1, tbar):
    tvals  = [float(T1 + i*(T-T1)/Nvals) for i in range(0, Nvals+1)]
    phivals = [varphi0*(time+tbar)**alpha for time in tvals]
    plt.plot(tvals, phivals, "-g", label=r'$\varphi(t)$', linewidth=2, markersize=0)
```

在 \([T_1, T]\) 上绘制 \(\varphi(t)\)（实线绿色），公式同上。

---

### 2.4 `plot_basicHermite` — 绘制基础 Hermite 多项式

```python
# fouker.py 第43–46行
def plot_basicHermite(Nvals, a, b, pC, degree, line_str, label_str):
    tvals  = [float(a + i*(b-a)/Nvals) for i in range(0, Nvals+1)]
    pCvals = [get_MappedHornerPolyValue(pC, a, b, time, degree) for time in tvals]
    plt.plot(tvals, pCvals, line_str, label=label_str, linewidth=2, markersize=0)
```

在 \([a, b]\) 上均匀采样 \(N_{\text{vals}}+1\) 个点，用 Horner 法求多项式值（见 [2.13 节](#sec-fouker-horner)）：

\[
t_i = a + i \cdot \frac{b-a}{N_{\text{vals}}}, \quad i = 0, 1, \ldots, N_{\text{vals}}
\]

计算值：\(p_C(t_i)\)（通过 `get_MappedHornerPolyValue`）。

---

### 2.5 `plot_DDHermite` — 绘制除差法 Hermite 插值

```python
# fouker.py 第49–53行
def plot_DDHermite(Nvals, a, b, z, DDmat, smoothness, line_str, label_str):
    tvals  = [float(a + i*(b-a)/Nvals) for i in range(0, Nvals+1)]
    pCvals = [get_DDHermiteValue(time, DDmat, smoothness, z) for time in tvals]
    plt.plot(tvals, pCvals, line_str, label=label_str, linewidth=2, markersize=0)
```

对 \([a, b]\) 上的均匀采样点，调用 `get_DDHermiteValue`（见 [2.12 节](#sec-fouker-getDDHV)）计算除差法 Newton 插值多项式：

\[
p(t) = \sum_{k=0}^{2m+1} [z_0,\ldots,z_k]\, (t - z_0)(t - z_1)\cdots(t - z_{k-1})
\]

其中 \([z_0,\ldots,z_k]\) 是除差（divided differences），\(z\) 是重复节点向量。

---

### 2.6 `plot_leftHermite` — 绘制左端 Hermite 多项式（旧接口）

```python
# fouker.py 第56–59行
def plot_leftHermite(Nvals, T1, pL):
    tvals  = [float(0. + i*(T1-0.)/Nvals) for i in range(0, Nvals+1)]
    pLvals = [np.polyval(np.flipud(pL), time) for time in tvals]
    plt.plot(tvals, pLvals, "-b", label=r'$p_L(t)$', linewidth=2, markersize=0)
```

在 \([0, T_1]\) 上绘制左端 Hermite 多项式 \(p_L(t)\)（蓝色实线）。  
`np.polyval(np.flipud(pL), time)` 等价于计算：

\[
p_L(t) = \sum_{k=0}^{2m+1} c_k\, t^k
\]

其中 `pL[k]` = \(c_k\)（低次项在前），`np.flipud` 将其反转为高次项在前，再调用 `np.polyval`。

---

### 2.7 `plot_rightHermite` — 绘制右端 Hermite 多项式（旧接口）

```python
# fouker.py 第62–65行
def plot_rightHermite(Nvals, T, Tx, pR):
    tvals  = [float(T + i*(Tx-T)/Nvals) for i in range(0, Nvals+1)]
    pRvals = [np.polyval(np.flipud(pR), time) for time in tvals]
    plt.plot(tvals, pRvals, "-r", label=r'$p_R(t)$', linewidth=2, markersize=0)
```

在 \([T, T_x]\) 上绘制右端 Hermite 多项式 \(p_R(t)\)（红色实线），公式同上。

---

### 2.8 `plot_FS` — 绘制 Fourier 级数代理

```python
# fouker.py 第68–72行
def plot_FS(Tx, Nvals, cnR, Nlim):
    tvals  = [float(0. + i*(Tx-0.)/Nvals) for i in range(0, Nvals+1)]
    FSvals = [sum([cnR[k+Nlim]*math.cos(math.pi*k*tvals[i]/Tx)
              for k in range(-Nlim, Nlim+1, 1)]) for i in range(0, Nvals+1, 1)]
    plt.plot(tvals, FSvals, "-k", label=r'$F\psi(t)$', linewidth=2, markersize=0)
    return FSvals
```

在 \([0, T_x]\) 上计算并绘制截断 Fourier 级数（黑色实线）：

\[
F\psi(t) = \sum_{k=-L}^{L} c_k \cos\!\left(\frac{\pi k t}{T_x}\right)
\]

因 \(\psi\) 为偶函数，虚部消失，`cnR[k+L]` = \(c_k\)（实部系数）。

---

### 2.8b `plot_long_FS` — 绘制 Fourier 级数的多周期延拓

```python
# fouker.py 第75–85行
def plot_long_FS(Nper, Tx, Nvals, cnR, Nlim, varphi0, alpha, T, tbar):
    tvals  = [float(-Nper*Tx + i*(2*Nper*Tx-0.)/Nvals) for i in range(0, Nvals+1)]
    FSvals = [sum([cnR[k+Nlim]*math.cos(math.pi*k*tvals[i]/Tx)
              for k in range(-Nlim, Nlim+1, 1)]) for i in range(0, Nvals+1, 1)]
    plt.plot(tvals, FSvals, "-k", ...)
    return FSvals
```

在 \([-N_{\text{per}} T_x,\; N_{\text{per}} T_x]\) 上绘制 Fourier 级数的周期延拓，时间跨越 \(2N_{\text{per}}\) 个周期。公式与 `plot_FS` 相同，但采样范围不同。

---

### 2.8c `get_FS` — 返回 Fourier 级数值向量

```python
# fouker.py 第88–91行
def get_FS(Tx, Nvals, cnR, Nlim):
    tvals  = [float(0. + i*(Tx-0.)/Nvals) for i in range(0, Nvals+1)]
    FSvals = [sum([cnR[k+Nlim]*math.cos(math.pi*k*tvals[i]/Tx)
              for k in range(-Nlim, Nlim+1, 1)]) for i in range(0, Nvals+1, 1)]
    return FSvals
```

不绘图，仅返回值向量。计算公式与 `plot_FS` 完全相同：

\[
F\psi(t_i) = \sum_{k=-L}^{L} c_k \cos\!\left(\frac{\pi k t_i}{T_x}\right)
\]

---

### 2.9 `get_basicHermite` — 矩阵法求 Hermite 系数（旧接口）

```python
# fouker.py 第94–113行
def get_basicHermite(n, f, fscale, loudness):
    A = np.zeros((2*n+2, 2*n+2))
    pC = np.zeros((2*n+2, 1))
    coeff = np.ones((2*n+2, 1))
    for row in range(0, 2*n+1, 2):
        apow = 1
        f[row  ] = f[row  ] * fscale**(row/2)
        f[row+1] = f[row+1] * fscale**(row/2)
        for col in range(row/2, 2*n+2, 1):
            A[row  , col] = coeff[col] * apow
            A[row+1, col] = coeff[col]
            coeff[col]    = coeff[col] * (col - row/2)
            apow = 0.0
    pC = np.linalg.solve(A, f)
    ...
    return pC
```

本函数在 \(\tau \in [0, 1]\) 上用矩阵法求 Hermite 插值多项式 \(p_C\) 的系数，然后通过缩放映射到真实区间 \([a, b]\)（缩放因子 `fscale` = \(b - a\)）。

Hermite 多项式具有 \(2(m+1)\) 个自由度（次数为 \(2m+1\)），约束条件为：

- 在 \(\tau = 0\)：\(p_C^{(k)}(0) = f[2k]\)，\(k = 0, 1, \ldots, m\)（其中对阶导数值做了 `fscale` 缩放）。  
- 在 \(\tau = 1\)：\(p_C^{(k)}(1) = f[2k+1]\)，\(k = 0, 1, \ldots, m\)。

矩阵 \(A\) 中，第 \(2r\) 行和第 \(2r+1\) 行分别对应在 \(\tau=0\) 和 \(\tau=1\) 处的第 \(r\) 阶导数约束。列对应多项式基底 \(\{1, \tau, \tau^2, \ldots, \tau^{2m+1}\}\)：

\[
A_{2r,\, c} = c(c-1)\cdots(c-r+1) \cdot 0^{c-r}, \quad
A_{2r+1,\, c} = c(c-1)\cdots(c-r+1) \cdot 1^{c-r}
\]

（此处 `coeff[col]` 在每次外层循环迭代中累积降次后的组合系数，`apow` 在第一次列迭代后置零，从而实现 \(0^{c-r}\) 的效果。）

求解线性系统 \(A\, \mathbf{p}_C = \mathbf{f}\) 得到系数向量：

\[
\mathbf{p}_C = A^{-1} \mathbf{f}
\]

---

<a id="sec-fouker-getDDH"></a>
### 2.10 `get_DDHermite` — 除差法 Hermite 插值矩阵

```python
# fouker.py 第141–166行
def get_DDHermite(a, b, m, fa, fb, da, db):
    DDmat = np.zeros((2*m+2, 2*m+2))
    z     = np.zeros(2*m+2)
    for k in range(0, m+1):
        z[k] = a;  z[m+1+k] = b
    for k in range(0, m+1):
        DDmat[k, k]     = fa
        DDmat[k+m+1, k+m+1] = fb
    nfac = 1
    for n in range(1, m+1):
        nfac = n * nfac
        for k in range(0, m+1-n):
            DDmat[k,   k+n]     = da[n] / nfac
            DDmat[m+k+1, m+k+n+1] = db[n] / nfac
    for k in range(m, -1, -1):
        for n in range(m+1, 2*m+2):
            DDmat[k, n] = ( DDmat[k+1, n] - DDmat[k, n-1] ) / (b - a)
    return DDmat, z
```

**除差法（Divided Differences）Newton 插值** 的核心数据结构。

节点向量（重复节点，保证在端点处满足 Hermite 插值条件）：

\[
z = \underbrace{a, a, \ldots, a}_{m+1}, \underbrace{b, b, \ldots, b}_{m+1}
\]

矩阵初始化：对 \(k = 0, 1, \ldots, m\)，

\[
[a, \ldots, a]_{k+1 \text{ 个}} = \frac{f^{(k)}(a)}{k!} \quad \Rightarrow \quad \text{DDmat}[k, k] = f_a
\]

（代码中 `DDmat[k,k] = fa` 表示 \([a]=f_a\)；`DDmat[k, k+n] = da[n]/n!` 表示 \([a,\ldots,a]_{n+1}\) 对角块。）

对于跨越 \(a\) 和 \(b\) 的混合除差，用标准递推公式（注意此处分母始终为 \(b - a\)）：

\[
[z_k, \ldots, z_{k+n}] = \frac{[z_{k+1}, \ldots, z_{k+n}] - [z_k, \ldots, z_{k+n-1}]}{z_{k+n} - z_k}
\]

代码中 `DDmat[k, n] = (DDmat[k+1, n] - DDmat[k, n-1]) / (b - a)`。

返回值：
- `DDmat`：上三角矩阵，其第一行包含 Newton 插值系数
- `z`：重复节点向量

---

<a id="sec-fouker-getDDHV"></a>
### 2.11 `get_DDHermiteValue` — 求 DD Hermite 多项式值

```python
# fouker.py 第169–175行
def get_DDHermiteValue(t, DDmat, m, z):
    total = 0.0
    coeff = 1.0
    for k in range(0, 2*m+2):
        total = total + DDmat[0, k] * coeff
        coeff = coeff * (t - z[k])
    return total
```

Newton 插值公式：

\[
p(t) = \sum_{k=0}^{2m+1} [z_0, z_1, \ldots, z_k] \cdot \omega_k(t)
\]

其中 \(\omega_k(t) = \prod_{j=0}^{k-1}(t - z_j)\)（\(\omega_0 = 1\)）。

代码中 `DDmat[0, k]` = \([z_0, \ldots, z_k]\)，`coeff` 在每次迭代中累乘 \((t - z_{k-1})\)，实现 \(\omega_k(t)\) 的递推计算。

---

<a id="sec-fouker-getpartialerror"></a>
### 2.12 `get_partialerror` — Fourier 级数在 \([T_1, T]\) 上的 \(L^\infty\) 误差

```python
# fouker.py 第208–222行
def get_partialerror(varphi0, alpha, Nvals, T1, T, tbar, cnR, Nlim, Tx):
    error = 0
    for i in range(0, Nvals+1, 1):
        t        = float(T1 + i*(T-T1)/Nvals)
        FSvals_i = sum([cnR[k+Nlim]*math.cos(math.pi*k*t/Tx) for k in range(-Nlim, Nlim+1, 1)])
        phivals_i = varphi0 * (t+tbar)**alpha
        error = max(abs(FSvals_i - phivals_i), error)
    return error
```

计算 Fourier 代理在 \([T_1, T]\) 上的最大绝对误差：

\[
\text{FSerror} = \max_{i=0,\ldots,N_{\text{vals}}} \left|
\sum_{k=-L}^{L} c_k \cos\!\left(\frac{\pi k t_i}{T_x}\right) - \varphi_0 (t_i + \bar{t})^\alpha
\right|
\]

其中 \(t_i = T_1 + i(T - T_1) / N_{\text{vals}}\)。

---

### 2.13 `get_HermiteInterceptValues`、`get_LeftHermiteInterceptValue`、`get_RightHermiteInterceptValue` — 截距值计算

```python
# fouker.py 第224–233行
def get_HermiteInterceptValues(lbeta, rbeta, varphi0, T1, T, Tx, tbar, alpha):
    lval = varphi0*(T1+tbar)**alpha - T1*lbeta*alpha*varphi0*(T1+tbar)**(alpha-1)
    rval = varphi0*(T+tbar)**alpha  + (1-rbeta)*(Tx-T)*alpha*varphi0*(T+tbar)**(alpha-1)
    return lval, rval

def get_LeftHermiteInterceptValue(varphi0, T1, tbar, alpha):
    return 1.4 * varphi0*(T1+tbar)**alpha

def get_RightHermiteInterceptValue(varphi0, T, tbar, alpha):
    return 0.95 * varphi0*(T+tbar)**alpha
```

**左截距值**（Hermite 多项式在 \(t=0\) 处的函数值）：

通过将切线向左延伸到 \(t=0\) 再按 \(\beta_L\) 做内插：

\[
p_L(0) = \varphi(T_1) - T_1 \cdot \beta_L \cdot \varphi'(T_1)
= \varphi_0(T_1+\bar{t})^\alpha - T_1 \beta_L \alpha \varphi_0 (T_1+\bar{t})^{\alpha-1}
\]

代码中 `lval` 对应 \(p_L(0)\)，`lbeta = 0.9`（在调用处固定）。

**右截距值**（Hermite 多项式在 \(t=T_x\) 处的函数值）：

\[
p_R(T_x) = \varphi(T) + (1-\beta_R)(T_x - T)\varphi'(T)
= \varphi_0(T+\bar{t})^\alpha + (1-\beta_R)(T_x-T)\alpha\varphi_0(T+\bar{t})^{\alpha-1}
\]

代码中 `rval` 对应 \(p_R(T_x)\)，`rbeta = 0.5`。

**简化截距界（仅用于绘图 ylim）**：

\[
\text{LeftIntercept} = 1.4\,\varphi_0 (T_1+\bar{t})^\alpha, \quad
\text{RightIntercept} = 0.95\,\varphi_0 (T+\bar{t})^\alpha
\]

---

<a id="sec-fouker-horner"></a>
### 2.14 `get_MappedHornerPolyValue` — 映射 Horner 法求多项式值

```python
# fouker.py 第275–280行
def get_MappedHornerPolyValue(pC, a, b, t, degree):
    tau   = (t - a) / (b - a)
    value = pC[degree]
    for k in range(degree-1, -1, -1):
        value = pC[k] + tau * value
    return value
```

**映射变量**：

\[
\tau = \frac{t - a}{b - a} \in [0, 1]
\]

**Horner 法**求多项式：

\[
p(\tau) = c_0 + \tau(c_1 + \tau(c_2 + \cdots + \tau(c_{d-1} + \tau\, c_d)\cdots))
\]

其中 \(d = \text{degree} = 2m+1\)，\(c_k = \text{pC}[k]\)。  
Horner 法将 \(d+1\) 次乘法和加法组合为 \(d\) 次乘加，避免了重复的幂次计算。

---

<a id="sec-fouker-getFCN"></a>
### 2.15 `get_FourierCoefficientsNew` — 计算 Fourier 级数系数（完整流程）

```python
# fouker.py 第282–357行
def get_FourierCoefficientsNew(tbar, T1, T, Tx, varphi0, alpha, smoothness, Nlim, DDuse, loudness):
    lval, rval = get_HermiteInterceptValues(0.9, 0.5, ...)

    # --- 左端 Hermite 插值 [0, T1] ---
    if DDuse == 1:
        fa = lval;  fb = varphi0*(T1+tbar)**alpha
        da = np.zeros(1+smoothness);  db = np.zeros(1+smoothness)
        deriv = alpha*fb/(T1+tbar)   # φ'(T1)
        for n in range(1, 1+smoothness):
            db[n] = deriv
            deriv = (alpha-n)*deriv/(T1+tbar)
        DDmatL, zL = get_DDHermite(0, T1, smoothness, fa, fb, da, db)
    else:
        # 矩阵法
        fL[0] = lval
        fL[1] = varphi0*(T1+tbar)**alpha
        for k in range(2, 2*smoothness+2, 2):
            fL[k]   = 0.0
            fL[k+1] = fL[k-1] * (alpha-(k-2.0)/2.0) * (T1+tbar)**(-1)
        pL = get_basicHermite(smoothness, fL, T1-0., loudness)

    # --- 右端 Hermite 插值 [T, Tx] ---
    if DDuse == 1:
        fa = varphi0*(T+tbar)**alpha;  fb = rval
        da = np.zeros(1+smoothness);  db = np.zeros(1+smoothness)
        deriv = alpha*fb/(T+tbar)
        for n in range(1, 1+smoothness):
            da[n] = deriv
            deriv = (alpha-n)*deriv/(T+tbar)
        DDmatR, zR = get_DDHermite(T, T+Tx, smoothness, fa, fb, da, db)
    else:
        ...
        pR = get_basicHermite(smoothness, fR, Tx-T, loudness)

    # --- Fourier 系数计算 ---
    cnR = np.zeros((2*Nlim+1, 1))
    for n in range(0, Nlim+1):
        # 左端 Hermite 部分
        realpart = quad(lambda t: pL(t)*cos(πnt/Tx), 0, T1)
        cnR[n+Nlim] = 1.0/Tx * realpart[0]
        # 中间幂律核部分
        realpart = quad(lambda t: φ(t)*cos(πnt/Tx), T1, T)
        cnR[n+Nlim] += 1.0/Tx * realpart[0]
        # 右端 Hermite 部分
        realpart = quad(lambda t: pR(t)*cos(πnt/Tx), T, Tx)
        cnR[n+Nlim] += 1.0/Tx * realpart[0]
    # 利用偶对称填充负指标系数
    for n in range(-Nlim, 0):
        cnR[n+Nlim] = cnR[-n+Nlim]

    if DDuse == 1:
        return cnR, zL, DDmatL, zR, DDmatR
    else:
        return cnR, pL, pR
```

**左端导数值（`da`, `db`）的计算**：

在 \(t = 0\) 处，所有导数为 0（使得 \(\psi\) 在 \(t=0\) 处光滑过渡）：

\[
p_L^{(k)}(0) = 0, \quad k = 1, 2, \ldots, m \quad \Rightarrow \quad da[k] = 0
\]

在 \(t = T_1\) 处，需匹配 \(\varphi\) 的各阶导数：

\[
p_L^{(k)}(T_1) = \varphi^{(k)}(T_1), \quad k = 0, 1, \ldots, m
\]

幂律函数 \(\varphi(t) = \varphi_0(t+\bar{t})^\alpha\) 的第 \(k\) 阶导数递推：

\[
\varphi'(t) = \alpha \varphi_0 (t+\bar{t})^{\alpha-1}, \quad
\varphi^{(k)}(t) = \frac{\alpha(\alpha-1)\cdots(\alpha-k+1)}{(t+\bar{t})^k}\, \varphi(t)
= \prod_{j=0}^{k-1}(\alpha - j) \cdot \varphi_0 (t+\bar{t})^{\alpha-k}
\]

代码中的递推：

```python
deriv = alpha * fb / (T1 + tbar)   # = φ'(T1)
db[n] = deriv                       # = φ^(n)(T1)
deriv = (alpha - n) * deriv / (T1 + tbar)  # = φ^(n+1)(T1)
```

即：

\[
\varphi^{(n)}(T_1) = \prod_{j=0}^{n-1}(\alpha - j) \cdot \varphi_0 (T_1+\bar{t})^{\alpha-n}
\]

**Fourier 系数公式**：

\(\psi(t)\) 为偶函数，故只需计算实部余弦系数：

\[
c_n = \frac{1}{T_x}\int_0^{T_x} \psi(t) \cos\!\left(\frac{\pi n t}{T_x}\right) dt
\]

分段积分：

\[
c_n = \frac{1}{T_x} \left[
\int_0^{T_1} p_L(t) \cos\!\left(\frac{\pi n t}{T_x}\right) dt
+ \int_{T_1}^{T} \varphi(t) \cos\!\left(\frac{\pi n t}{T_x}\right) dt
+ \int_{T}^{T_x} p_R(t) \cos\!\left(\frac{\pi n t}{T_x}\right) dt
\right]
\]

对称性：

\[
c_{-n} = c_n, \quad n = 1, 2, \ldots, L
\]

所以代码中先算 \(n = 0, 1, \ldots, L\)，再用 `cnR[n+Nlim] = cnR[-n+Nlim]` 填充负指标。

---

### 2.16 `get_FSConvergenceResults` — Fourier 级数收敛性验证

```python
# fouker.py 第360–433行
def get_FSConvergenceResults(varphi0, alpha, tbar, T, T1, Tx, Nvals, cnR, Nlim, smoothness, gfx, old_way, loudness):
    max_m = 10; max_L = 12
    errors = np.zeros((max_L, max_m))
    ratios = np.zeros((max_L, max_m))
    for i in range(0, max_L):
        Nlim = 2**i
        for m in range(1, max_m):
            smoothness = m
            # 重新计算 Fourier 系数
            cnR, ... = get_FourierCoefficientsNew(...)
            FSerror  = get_partialerror(...)
            errors[i, m] = FSerror
    for i in range(1, max_L):
        for m in range(1, max_m):
            ratios[i, m] = errors[i-1, m] / errors[i, m]
    # 打印 LaTeX 表格与绘图
    ...
```

对 \(L = 2^0, 2^1, \ldots, 2^{11}\) 与 \(m = 1, 2, \ldots, 9\) 的所有组合，计算 Fourier 代理误差：

\[
e(L, m) = \max_{t \in [T_1, T]} \left| F_L \psi_m(t) - \varphi(t) \right|
\]

其中 \(F_L\) 表示截断到 \(L\) 项的 Fourier 级数，\(\psi_m\) 是 \(C^m\) 光滑度的代理函数。

收敛比（相邻 \(L\) 翻倍时的误差比）：

\[
r(L, m) = \frac{e(L/2,\, m)}{e(L,\, m)}
\]

若 \(e(L, m) = O(L^{-p})\)，则 \(r \approx 2^p\)，即从 \(r\) 可以读出收敛阶 \(p\)。

绘图时在双对数坐标下绘制 \(e(L, m)\) vs \(L\) 曲线，\(x\) 轴为 \(L\)，\(y\) 轴为 \(\|(I - F_L)\varphi\|_{L^\infty(T_1,T)}\)。

---

<a id="plotter"></a>
## 三、plotter.py —— 绘图脚本

`plotter.py` 是独立脚本，读取 `fouvol.py` 批量运行产生的结果文件，绘制误差/时间曲线。

### 3.1 依赖与初始化

```python
# plotter.py 第20–31行
import numpy as np
import scipy.signal
import os, math
import matplotlib as mpl
...
import matplotlib.pyplot as plt
import itertools
import commands
```

`commands.getoutput`（Python 2）执行 shell 命令并返回输出字符串，用于读取 `Lmin`（Fourier 截断项数起始值）。

### 3.2 读取 Lmin

```python
# plotter.py 第35行
Lmin = int(commands.getoutput(
    "head -3 errortable.raw | tail -1 | tr '&' ';' | tr -s ' ' | cut -d';' -f2 | cut -d' ' -f2"))
```

从 `errortable.raw` 文件（由 `fouvol.py` 批量运行产生的 LaTeX 表格格式）中提取第一行数据的 \(L\) 值，作为绘图图例的起始值：

\[
L_{\min} = \text{第一数据行第二列的整数值}
\]

### 3.3 数据文件预处理

```python
# plotter.py 第41–42行
os.system("cat errortable.raw | sed '1,3d' | sed '$d' | tr '&' ' ' | sed 's/\\\\//g' > errortable.dat")
os.system("cat timestable.raw | sed '1,3d' | sed '$d' | tr '&' ' ' | sed 's/\\\\//g' > timestable.dat")
```

Shell 命令流程：
1. `sed '1,3d'`：删除 LaTeX 表头的前 3 行
2. `sed '$d'`：删除最后一行（LaTeX 结尾 `\hline\end{tabular}`）
3. `tr '&' ' '`：将列分隔符 `&` 替换为空格
4. `sed 's/\\//g'`：删除反斜杠

产生纯数值格式的数据文件 `errortable.dat` 和 `timestable.dat`。

### 3.4 读取数据矩阵

```python
# plotter.py 第46–73行
fe = open('errortable.dat', 'r')
ft = open('timestable.dat', 'r')
Nc = len(fe.readline().split())   # 列数（第一列为 Nt，其余为各 L 值对应的误差）
Nl = 1
for line in fe:
    Nl = Nl + 1                   # 统计行数
fe.seek(0, 0)
errvals = np.zeros((Nl, Nc))
timvals = np.zeros((Nl, Nc))
linecount = 0
for line in fe:
    columns = line.split()
    for col in range(0, Nc):
        errvals[linecount, col] = float(columns[col])
    linecount += 1
# 同样处理 ft → timvals
```

读取后：
- `errvals[i, 0]` = \(N_t\)（第 \(i\) 行的时间步数）
- `errvals[i, j]`（\(j \ge 1\)）= \(|u(T) - U_{N_t}|\)（对应某个 \(L = L_{\min} \cdot 2^{DL(j-1)}\)）
- `timvals[i, j]`：对应的运行时间（秒）

### 3.5 绘制误差曲线

```python
# plotter.py 第81–107行
for col in range(1, Nc):
    plt.loglog(errvals[:,0], errvals[:,col],
               marker=marker.next(), linestyle='-', color='black',
               linewidth=2, markersize=8,
               label="$L = "+str(Lmin*2**(DL*(col-1)))+"$")
plt.legend(loc="lower left")
plt.xlim(xlo, xhi)
plt.ylim(0.000000000001, 10.0)
plt.savefig('errors.png', ...)
plt.savefig('errors.eps', ...)
```

在双对数坐标下绘制 \(N_t\) vs 误差曲线。理想情况下，对乘积矩形规则应有

\[
|u(T) - U_{N_t}| = O(\Delta t) = O(N_t^{-1})
\]

即双对数图上斜率约为 \(-1\)。对乘积梯形规则（solver=2,4），理论上

\[
|u(T) - U_{N_t}| = O(\Delta t^{1+\alpha})
\]

图例中 \(L = L_{\min} \cdot 2^{DL(j-1)}\) 标注每条曲线对应的 Fourier 截断项数。

### 3.6 绘制时间曲线

```python
# plotter.py 第109–135行
for col in range(1, Nc):
    plt.loglog(timvals[:,0], timvals[:,col], ...)
plt.savefig('timings.png', ...)
plt.savefig('timings.eps', ...)
```

绘制 \(N_t\) vs 运行时间（秒）的双对数图：
- 对朴素求积（solver=1,2）：时间 \(\sim O(N_t^2)\)，斜率约为 2。
- 对 Fourier 递归（solver=3,4）：因近端求积量固定为 \(N_1\) 步，斜率可能更接近 1。

---

<a id="compare"></a>
## 四、compare.py —— 比较脚本

`compare.py` 读取两个不同目录下的结果（参考数据和比较数据），在同一图上绘制误差 vs 时间曲线，用于比较两种算法的效率。

### 4.1 依赖与命令行解析

```python
# compare.py 第21–88行
import numpy as np, scipy.signal, os, sys, getopt, time, math
import matplotlib as mpl, matplotlib.pyplot as plt
import itertools, commands

def usage():
    print("-r s or --reference s   directory containing reference data")
    print("-c s or --comparison s  directory containing reference data")
    print("-X s or --gfx s         to block/hold screen gfx")
    ...

try:
    opts, args = getopt.getopt(sys.argv[1:], "hr:c:X:", ["help","reference=","comparison=","gfx="])
except getopt.GetoptError as err:
    ...

reference  = './'
comparison = './'
gfx        = 0
DL         = 2
for o, a in opts:
    if o in ("-r","--reference"):
        reference = str(a)
    elif o in ("-c","--comparison"):
        comparison = str(a)
    elif o in ("-X","--gfx"):
        gfx = float(a)
    ...
```

参数：
- `reference`：包含参考结果的目录路径
- `comparison`：包含比较结果的目录路径

### 4.2 读取参考数据

```python
# compare.py 第97–131行
cwd = os.getcwd()
os.chdir(reference)
rfe = open('errortable.dat', 'r')
rft = open('timestable.dat', 'r')
Nc = len(rfe.readline().split())
Nl = 1
for line in rfe:
    Nl = Nl + 1
rfe.seek(0, 0)
rerrvals = np.zeros((Nl, Nc))
rtimvals = np.zeros((Nl, Nc))
# ... 读取循环 ...
rfe.close(); rft.close()
```

与 `plotter.py` 的读取逻辑相同，读入参考数据矩阵 `rerrvals`，`rtimvals`。

### 4.3 读取比较数据

```python
# compare.py 第135–173行
os.chdir(cwd); os.chdir(comparison)
Lmin = int(commands.getoutput("head -3 errortable.raw | tail -1 | ..."))
cfe = open('errortable.dat', 'r')
cft = open('timestable.dat', 'r')
# ... 同样读取循环 ...
cerrvals = np.zeros((Nl, Nc))
ctimvals = np.zeros((Nl, Nc))
```

### 4.4 绘制误差-时间对比图

```python
# compare.py 第180–215行
plt.loglog(rerrvals[:,1], rtimvals[:,1],
           marker=marker.next(), linestyle='--', color='black',
           label="reference")
for col in range(1, Nc):
    plt.loglog(cerrvals[:,col], ctimvals[:,col],
               marker=marker.next(), linestyle='-', color='black',
               label="$L = "+str(Lmin*2**(DL*(col-1)))+"$")
plt.xlim(0.0000001, 1000)
plt.ylim(0.1, 100000)
plt.savefig('compare.png', ...)
plt.savefig('compare.eps', ...)
```

在双对数坐标下绘制**误差（横轴）vs 运行时间（纵轴）**的 Pareto 前沿比较：
- 虚线（`--`）为参考算法
- 实线（`-`）为比较算法的不同 \(L\) 值

这种"误差-时间"双对数图（efficiency plot）的含义：

\[
\text{效率} \propto \frac{1}{\text{误差} \times \text{时间}}
\]

**曲线越靠左下方**（误差小且运行时间短）表示算法效率越高。若 Fourier 递归求解器的曲线位于参考曲线的左下方，说明在相同精度下运行更快。

---

## 附录：符号对照总表

| 代码变量 | 数学符号 | 含义 |
|---------|---------|------|
| `varphi0` | \(\varphi_0\) | 幂律核系数 |
| `alpha` | \(\alpha\) | 幂律指数，\(\alpha \in (-1,0)\) |
| `tbar` | \(\bar{t}\) | 核函数时移参数 |
| `T` | \(T\) | 最终时刻 |
| `T1` | \(T_1\) | 近端求积上限 |
| `Tx` | \(T_x\) | Fourier 级数半周期，\(T_x = T + T_1\) |
| `dt` | \(\Delta t\) | 时间步长，\(\Delta t = T/N_t\) |
| `Nt` | \(N_t\) | 时间步数 |
| `N1` | \(N_1\) | 近端步数，\(N_1 = \lceil T_1/\Delta t \rceil\) |
| `Nlim` | \(L\) | Fourier 级数截断项数 |
| `smoothness` | \(m\) | Hermite 插值 \(C^m\) 光滑度 |
| `uvals[n]` | \(U_n\) | \(u(t_n)\) 的数值近似 |
| `cnR[k+L]` | \(c_k\) | Fourier 余弦系数 |
| `phistarR[k+L]` | \(\mathrm{Re}(\Phi_k^*)\) | 第 \(k\) 模式远端历史实部 |
| `phistarI[k+L]` | \(\mathrm{Im}(\Phi_k^*)\) | 第 \(k\) 模式远端历史虚部 |
| `hist` | \(\text{hist}\) | Volterra 历史积分的数值近似 |
| `denom` | \(\text{denom}\) | 当前时刻未知量 \(U_n\) 的系数（分母） |
| `DDmat` | \([z_0,\ldots,z_k]\) | 除差矩阵（Newton 插值系数） |
| `z` | \(z\) | 重复节点向量 |
| `error` | \(\|u(T)-U_{N_t}\|\) | 最终时刻绝对误差 |
| `FSerror` | FSerror | Fourier 代理在 \([T_1,T]\) 上的 \(L^\infty\) 误差 |

---

*本文档覆盖了 fouvol.py（449行）、fouker.py（434行）、plotter.py（136行）、compare.py（217行）中的全部代码，按主函数运行顺序逐段给出代码（含行号）与对应数学公式。*
