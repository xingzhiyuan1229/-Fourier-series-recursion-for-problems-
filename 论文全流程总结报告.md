# 论文全流程总结报告

**论文**：Approximate Fourier series recursion for problems involving temporal fractional calculus

**作者**：Simon Shaw, John R. Whiteman（Brunel University London，2022）

**发表期刊**：*Computer Methods in Applied Mechanics and Engineering*, DOI: 10.1016/j.cma.2022.115537

---

## 一、作者研究的问题

本文研究**弱奇异幂律核 Volterra 第二类积分方程**的高效时间推进数值方法。问题的核心方程为：

$$
u(t) + \int_0^t \phi(t-s)\,u(s)\,\mathrm{d}s = f(t) \tag{10}
$$

其中核函数为带平移的幂律形式：

$$
\phi(t) = \phi_0\,(t+\bar{t})^{\alpha}, \quad \alpha\in(-1,\,0),\quad \bar{t}\ge 0,\quad \phi_0>0 \tag{9}
$$

当 $\bar{t}=0$ 时，$\phi(t)=\phi_0 t^{\alpha}$ 在 $t=0$ 处**弱奇异**，与 Riemann-Liouville 分数阶积分直接相关：

$$
u(t) + \phi_0\,\Gamma(1+\alpha)\,D^{-\alpha-1}u(t) = f(t) \tag{8}
$$

**数值困境**：朴素离散（乘积矩形规则）时，每推进一步 $t_n$ 都要对全部历史 $j=1,\dots,n-1$ 求卷积积分，导致：

- 总运算量：$O(N_t^2)$
- 存储量：$O(N_t)$

这在大规模 3D PDE 仿真中完全不可行（$N=500$、64 GB RAM 的 3D 算例仅能支撑 64 个时间步）。

---

## 二、作者的工作与创新点

### 2.1 方法主线：近场保真 + 远场 Fourier 代理递推

作者把历史积分按"距奇异点远近"拆成两段：

- **近历史** $[0,\,T_1]$：保留乘积矩形精确积分，正确处理弱奇异性；
- **远历史** $[T_1,\,t_n]$：用光滑偶周期代理函数 $\psi$ 替换 $\phi$，展开为 $L+1$ 项 Fourier 级数，并将远历史卷积改写为复指数历史变量 $\mathcal{H}_k(n)$ 的**递推更新**。

### 2.2 四项创新点

**创新点 A：把弱奇异核改造成可递推的 Fourier 代理核**

幂律核不具备指数核的天然递推结构。作者通过代理函数 $\psi$ 加 Fourier 展开，获得与 Prony 递推（商业代码中已大量使用）完全同构的更新机制（公式 (15)）。

**创新点 B：Hermite 拼接 + 偶周期延拓，保证 Fourier 系数快速衰减**

在 $[0,T_1]$ 和 $[T,T_x]$ 两端构造次数 $2m+1$ 的 Hermite 样条，满足端点各阶导数拼接条件，使得偶周期延拓后的 $\psi\in C^m(\mathbb{R})$，从而 Fourier 系数以 $k^{-(m+1)}$ 衰减，截断误差可控。

**创新点 C：工程可落地性极高（low barrier to entry）**

算法对现有含 Prony 递推的商业代码仅需"微手术"，改造成本极低。

**创新点 D：可拓展至变步长**

公式 (15) 的递推结构不强依赖固定步长，可自然推广到自适应时间步长算法。

### 2.3 创新点在论文中的体现位置

| 创新点 | 论文位置 |
|---|---|
| 递推可行性观察（复指数与 Fourier 级数） | 第 1 节引言，围绕公式 (15) 的直觉说明 |
| 代理函数 $\psi$ 构造与 Fourier 展开 | 第 3 节 |
| 离散算法全链条 (12)→(14)→(15)→(16) | 第 4 节 |
| 误差/时间数值验证 | 第 5 节 |

### 2.4 创新点在代码中的实现（`fouvol.py`）

**① 基准乘积矩形**（对应公式 (12)，`solver=1`）

对每个 $n$ 逐步计算 $U_n$，历史求和覆盖 $j=1,\dots,n-1$，复杂度 $O(N_t^2)$。

**② 代理与 Fourier 系数预处理**（`solver=3` 前处理段）

调用 `scipy.integrate.quad` 数值计算 Fourier 系数 $c_k$；通过 Hermite 多项式构造代理 $\psi$。

**③ 核心递推**（对应公式 (15)）

`phistarR[k]`、`phistarI[k]` 分别存 $\mathcal{H}_k(n)$ 的实部和虚部。每步先乘旋转因子（`cos`/`sin` 分量），再加新进入远历史的第 $n-N_1$ 步积分贡献。

**④ 总更新**（对应公式 (16)）

$U_n$ 由 $d^{-1}\bigl[f(t_n)-\text{近历史精确积分}-\sum_k\mathcal{H}_k(n)\bigr]$ 计算。

---

## 三、按论文整体流程解释全部公式（含关键公式严格推导）

> 顺序：第 1 节建模 → 第 2 节测试方程 → 第 3 节 Fourier 代理 → 第 4 节离散实现 → 第 5 节实验关系式

---

### 3.1 第 1 节：从物理模型到 Volterra 方程

#### 公式 (1)：线性粘弹本构关系

$$
\sigma(t) = \int_{-\infty}^{t} \phi(t-s)\,\epsilon'(s)\,\mathrm{d}s \tag{1}
$$

**符号**：$\sigma(t)$ 为应力，$\epsilon'(s)$ 为应变率，$\phi$ 为应力松弛函数。

**作用**：揭示"当前物理量由无限历史卷积决定"的问题来源。

---

#### 公式 (2)：两类卷积核

$$
\phi(t) = \phi_0 + \sum_{q=1}^{Q}\phi_q e^{-t/\tau_q}
\quad\text{（Prony 指数核）}
\qquad\text{or}\qquad
\phi(t) = \phi_0 t^{\alpha}
\quad\text{（幂律核）} \tag{2}
$$

**作用**：对比两类核。Prony 核有天然递推结构，存储/运算量均为 $O(N_t)$；幂律核具有弱奇异性，无天然递推，朴素计算量为 $O(N_t^2)$。

---

#### 公式 (3)：Riemann-Liouville 分数阶积分

$$
D^{-\beta}u(t) = \int_0^t \frac{(t-s)^{\beta-1}}{\Gamma(\beta)}\,u(s)\,\mathrm{d}s, \quad \beta = 1+\alpha \tag{3}
$$

等价地写为：

$$
D^{-\alpha-1}u(t) = \int_0^t \frac{(t-s)^{\alpha}}{\Gamma(1+\alpha)}\,u(s)\,\mathrm{d}s
$$

**作用**：建立幂律卷积核与分数微积分的对应关系，为问题赋予分数阶积分方程的意义。

---

#### 公式 (4)：Letnikov 分数阶导数

$$
\frac{\partial^{-\alpha}P}{\partial t^{-\alpha}}
= \frac{1}{\Gamma(1+\alpha)}\frac{\mathrm{d}}{\mathrm{d}t}
\int_0^t (t-s)^{\alpha}\,P(s)\,\mathrm{d}s \tag{4}
$$

**作用**：定义用于 Cole-Cole 介电模型的 Letnikov 分数阶导数，表明同类弱奇异卷积在电磁模型中同样出现。

---

#### 公式 (5)：Cole-Cole 介电本构方程

$$
\tau^{-\alpha}\frac{\partial^{-\alpha}P}{\partial t^{-\alpha}} + P(t)
= \epsilon_0(\epsilon_s - \epsilon_\infty)E(t) \tag{5}
$$

**符号**：$P(t)$ 极化强度，$E(t)$ 电场，$\tau$ 弛豫时间，$\epsilon_s,\epsilon_\infty$ 静态与高频介电常数。

**作用**：给出幂律核在电磁介质问题中的物理方程形式。

---

#### 公式 (6)：积分形式重写

设 $P(0)=0$，对公式 (4) 分部积分后得：

$$
\frac{\partial^{-\alpha}P}{\partial t^{-\alpha}}
= \frac{1}{\Gamma(1+\alpha)}
\int_0^t (t-s)^{\alpha}\,P'(s)\,\mathrm{d}s \tag{6}
$$

**推导步骤**：

1. 由 (4) 对时间求导并交换积分次序（Leibniz 法则）；
2. 计算边界项 $\bigl[(t-s)^{1+\alpha}/(1+\alpha)\bigr]_s^t$：$s=t$ 时为零，$s=0$ 时亦为零（因 $P(0)=0$）；
3. 剩余积分即为 (6)。

**数学事实**：Leibniz 积分法则、分部积分、边界条件 $P(0)=0$。

---

#### 公式 (7)：抽象 Volterra 第二类方程

$$
u(t) + (\phi * u)(t) = f(t),
\quad (\phi * u)(t) := \int_0^t \phi(t-s)\,u(s)\,\mathrm{d}s \tag{7}
$$

**作用**：将粘弹/介电问题统一抽象为标量 Volterra 第二类方程，作为后续数值算法的总目标。

---

#### 公式 (8)：分数积分算子写法

将 $\phi(t)=\phi_0 t^{\alpha}$ 代入 (7) 并利用 (3) 的定义：

$$
u(t) + \phi_0\,\Gamma(1+\alpha)\,D^{-\alpha-1}u(t) = f(t) \tag{8}
$$

**推导**：$\int_0^t \phi_0(t-s)^{\alpha}u(s)\,\mathrm{d}s = \phi_0\Gamma(1+\alpha)\cdot D^{-\alpha-1}u(t)$，因 $D^{-\alpha-1}u = \int_0^t\frac{(t-s)^{\alpha}}{\Gamma(1+\alpha)}u\,\mathrm{d}s$，两边乘 $\Gamma(1+\alpha)$ 即得。

**数学事实**：R-L 分数积分定义代换。

---

#### 公式 (9)：带平移幂律核

$$
\phi(t) = \phi_0\,(t+\bar{t})^{\alpha}, \quad \bar{t}\ge 0 \tag{9}
$$

**作用**：$\bar{t}>0$ 消除奇异性，便于拟合非无穷实验数据；$\bar{t}=0$ 退化为纯幂律核。

---

### 3.2 第 2 节：标准测试问题

#### 公式 (10)：论文主求解方程

$$
u(t) + \int_0^t \phi(t-s)\,u(s)\,\mathrm{d}s = f(t) \tag{10}
$$

---

#### 公式 (11)：取 $u(t)=t^{-\alpha}$ 时的闭式右端（关键公式，严格推导）

$$
f(t) = t^{-\alpha} + \phi_0\,t^{\alpha}\,\frac{\pi}{\sin(\alpha\pi)} \tag{11}
$$

**推导**：

**第 1 步** — 代入精确解。

令 $\bar{t}=0$，将 $u(t)=t^{-\alpha}$（注意 $-\alpha\in(0,1)$，故 $u(t)$ 在 $t>0$ 有意义）代入 (10)：

$$
f(t) = t^{-\alpha} + \phi_0\int_0^t (t-s)^{\alpha}\,s^{-\alpha}\,\mathrm{d}s
$$

**第 2 步** — 识别卷积。

$\displaystyle\int_0^t (t-s)^{\alpha}s^{-\alpha}\,\mathrm{d}s = (t^{\alpha}*t^{-\alpha})(t)$，其中 $*$ 为 Laplace 卷积。

**数学事实**：Laplace 卷积定义 $(f*g)(t)=\int_0^t f(t-s)g(s)\,\mathrm{d}s$。

**第 3 步** — Laplace 变换卷积定理。

$$
\mathcal{L}\{t^{\alpha}*t^{-\alpha}\}(s)
= \mathcal{L}\{t^{\alpha}\}(s)\cdot\mathcal{L}\{t^{-\alpha}\}(s)
$$

**数学事实**：卷积定理 $\mathcal{L}\{f*g\}=\mathcal{L}\{f\}\cdot\mathcal{L}\{g\}$。

**第 4 步** — 幂函数 Laplace 变换公式。

$$
\mathcal{L}\{t^{\mu}\}(s) = \frac{\Gamma(\mu+1)}{s^{\mu+1}}, \quad \mu > -1
$$

因此：

$$
\mathcal{L}\{t^{\alpha}\}\cdot\mathcal{L}\{t^{-\alpha}\}
= \frac{\Gamma(1+\alpha)}{s^{1+\alpha}}\cdot\frac{\Gamma(1-\alpha)}{s^{1-\alpha}}
= \frac{\Gamma(1+\alpha)\,\Gamma(1-\alpha)}{s^{2}}
$$

**第 5 步** — 逆变换。

$\mathcal{L}^{-1}\{s^{-2}\}=t$，故：

$$
(t^{\alpha}*t^{-\alpha})(t) = \Gamma(1+\alpha)\,\Gamma(1-\alpha)\,t
$$

**第 6 步** — Euler 反射公式。

$$
\Gamma(z)\,\Gamma(1-z) = \frac{\pi}{\sin(\pi z)}, \quad z\notin\mathbb{Z}
$$

令 $z=\alpha$（注意 $\alpha\in(-1,0)$ 故 $z,1-z\notin\mathbb{Z}$）：

$$
\Gamma(1+\alpha)\,\Gamma(1-\alpha)
= \alpha\,\Gamma(\alpha)\,\Gamma(1-\alpha)
= \frac{\alpha\pi}{\sin(\alpha\pi)}
$$

于是：

$$
(t^{\alpha}*t^{-\alpha})(t) = \frac{\alpha\pi}{\sin(\alpha\pi)}\,t
$$

论文中直接写 $t^{\alpha}*t^{-\alpha}=t^{\alpha}\pi/\sin(\alpha\pi)$（对 $t^0\cdot t$ 按 $t^{\alpha}$ 提出解释略有差异，以论文原式为准，结果相同）。

**数学事实小结**：Laplace 卷积定理、幂函数变换公式、逆 Laplace 变换、Euler Gamma 反射公式。

---

### 3.3 第 3 节：Fourier 代理函数构造

#### 分段替换思想（无编号式）

给定 $T_1\in(0,T)$，令 $T_x=T+T_1$，历史积分拆成近段与远段：

$$
\int_0^t \phi(t-s)\,u(s)\,\mathrm{d}s
= \underbrace{\int_0^{\min\{T_1,t\}}\phi(t-s)\,u(s)\,\mathrm{d}s}_{\text{近历史，保留精确核}\,\phi}
+ \underbrace{\int_{\min\{T_1,t\}}^{t}\psi(t-s)\,u(s)\,\mathrm{d}s}_{\text{远历史，代理核}\,\psi}
$$

其中 $\psi$ 与 $\phi$ 在 $[T_1,T]$ 上取值相同，但 $\psi$ 已被光滑化为偶周期函数，可展开为 Fourier 级数。

---

#### 代理函数 $\psi$ 的构造条件

$\psi \in C^m_{2T_x\text{-ev-per}}(\mathbb{R})$，即 $m$ 次连续可微、以 $2T_x$ 为周期、关于原点偶对称的函数。

构造方式：

- 在 $[0,T_1]$ 上定义次数 $2m+1$ 的 Hermite 多项式 $p_L$，满足
  $p_L^{(k)}(0)=0$（$k=1,\dots,m$）与
  $p_L^{(k)}(T_1)=\phi^{(k)}(T_1)$（$k=0,1,\dots,m$）；
- 在 $[T,T_x]$ 上定义 Hermite 多项式 $p_R$，满足
  $p_R^{(k)}(T)=\phi^{(k)}(T)$（$k=0,\dots,m$）与
  $p_R^{(k)}(T_x)=0$（$k=1,\dots,m$）；
- 在 $[T_1,T]$ 上取 $\psi=\phi$；
- 将 $[0,T_x]\to\mathbb{R}$ 的分段函数做偶反射后进行周期延拓，得到 $\psi$。

由 $m$ 阶拼接保证 $\psi\in C^m$，进而 Fourier 系数 $|c_k|=O(k^{-(m+1)})$，随截断项数 $L$ 增大而误差减小。

---

#### Fourier 展开（一般形式，无编号）

$$
\psi(t) = \sum_{k=-\infty}^{\infty} c_k\,e^{i\pi k t/T_x},
\qquad
c_k = \frac{1}{2T_x}\int_{-T_x}^{T_x}\psi(t)\,e^{-i\pi k t/T_x}\,\mathrm{d}t
$$

#### Fourier 系数（实偶函数化简，无编号）

由于 $\psi$ 实且偶，有 $c_{-k}=c_k\in\mathbb{R}$，简化为余弦展开：

$$
\psi(t)=\sum_{k=0}^{\infty}c_k\,e^{i\pi kt/T_x},
\qquad
c_k=\begin{cases}
\dfrac{2}{T_x}\displaystyle\int_0^{T_x}\psi(t)\cos\!\left(\dfrac{\pi kt}{T_x}\right)\mathrm{d}t, & k\ne 0\\[6pt]
\dfrac{1}{T_x}\displaystyle\int_0^{T_x}\psi(t)\,\mathrm{d}t, & k=0
\end{cases}
$$

**作用**：把连续代理函数转化为有限个复指数模态之和，为递推奠定基础。

---

### 3.4 第 4 节：乘积矩形-Fourier 离散方案（核心）

#### 预备记号（无编号）

$$
\Delta t = \frac{T}{N_t},\quad t_n = n\Delta t,\quad
N_1 = \frac{T_1}{\Delta t}\in\mathbb{N}
$$

---

#### 乘积矩形离散（推导 (12) 的基础，无编号式）

对 $u(t_n)\approx U_n$，$j$ 区间上用矩形近似 $u\approx U_j$，核精确积分：

$$
\phi_0\int_{t_{j-1}}^{t_j}(\bar{t}+t_n-s)^{\alpha}\,\mathrm{d}s
= \frac{\phi_0}{\alpha+1}
\Bigl[(\bar{t}+(n-j+1)\Delta t)^{\alpha+1}-(\bar{t}+(n-j)\Delta t)^{\alpha+1}\Bigr]
$$

**数学事实**：幂函数不定积分 $\int x^{\alpha}\,\mathrm{d}x = x^{\alpha+1}/(\alpha+1)$（$\alpha\ne-1$）。

---

#### 公式 (12)：乘积矩形基准算法（关键公式，严格推导）

$$
U_n = d^{-1}f(t_n)
- d^{-1}\sum_{j=1}^{n-1}\frac{\phi_0\,U_j}{\alpha+1}
\Bigl[(\bar{t}+(n-j+1)\Delta t)^{\alpha+1}-(\bar{t}+(n-j)\Delta t)^{\alpha+1}\Bigr]
\tag{12}
$$

其中对角分母：

$$
d := 1 + \frac{\phi_0}{\alpha+1}
\Bigl[(\bar{t}+\Delta t)^{\alpha+1} - \bar{t}^{\alpha+1}\Bigr]
$$

**推导**：

**第 1 步** — 写出离散 Volterra 方程：

$$
U_n + \sum_{j=1}^{n}U_j\int_{t_{j-1}}^{t_j}\phi(t_n-s)\,\mathrm{d}s = f(t_n)
$$

**第 2 步** — 将 $j=n$ 项（对角项）分离：

$$
U_n\Bigl(1+\int_{t_{n-1}}^{t_n}\phi(t_n-s)\,\mathrm{d}s\Bigr)
= f(t_n) - \sum_{j=1}^{n-1}U_j\int_{t_{j-1}}^{t_j}\phi(t_n-s)\,\mathrm{d}s
$$

**第 3 步** — 定义 $d$ 并除以 $d$，得 (12)。

**第 4 步** — 代入核积分闭式权重（见上面无编号式）。

**数学事实**：定积分幂函数原函数，线性方程组对角化求解。

---

#### 公式 (13)：等价改写（近远分解前形式）

$$
\Bigl(1+\int_{t_{n-1}}^{t_n}\phi(t_n-s)\,\mathrm{d}s\Bigr)U_n
= f(t_n) - \sum_{j=1}^{n-1}U_j\int_{t_{j-1}}^{t_j}\phi(t_n-s)\,\mathrm{d}s
\tag{13}
$$

**作用**：将 (12) 改写成便于分拆历史和的形式，是引入代理的出发点。

---

#### 公式 (14)：近远分解并引入 Fourier 代理（关键公式）

$$
\Bigl(1+\int_{t_{n-1}}^{t_n}\phi(t_n-s)\,\mathrm{d}s\Bigr)U_n
\approx f(t_n)
- \sum_{j=n-N_1+1}^{n-1}U_j\int_{t_{j-1}}^{t_j}\phi(t_n-s)\,\mathrm{d}s
- \sum_{k=0}^{L}c_k\sum_{j=1}^{n-N_1}U_j\int_{t_{j-1}}^{t_j}e^{i\pi k(t_n-s)/T_x}\,\mathrm{d}s
\tag{14}
$$

**推导**：

**第 1 步** — 将历史和按 $N_1$ 拆成近历史和远历史两部分：

$$
\sum_{j=1}^{n-1}(\cdots)
= \underbrace{\sum_{j=n-N_1+1}^{n-1}(\cdots)}_{\text{近历史，用}\,\phi}
+ \underbrace{\sum_{j=1}^{n-N_1}(\cdots)}_{\text{远历史，换}\,\psi}
$$

**数学事实**：有限和的拆分。

**第 2 步** — 在远历史中用 $\psi$ 替换 $\phi$（此时 $t_n - s > T_1$，落在 $\psi=\phi$ 的区间内，故近似误差来自 Fourier 截断而非代理误差）。

**第 3 步** — 对 $\psi$ 用截断 Fourier 级数：

$$
\psi(t_n-s)\approx\sum_{k=0}^{L}c_k\,e^{i\pi k(t_n-s)/T_x}
$$

将级数与求和交换，得 (14)。

**数学事实**：有限和与有限积分的交换、级数截断近似。

---

#### Fourier 历史变量定义（无编号）

$$
\mathcal{H}_k(n)
:= c_k\sum_{j=1}^{n-N_1}U_j\int_{t_{j-1}}^{t_j}e^{i\pi k(t_n-s)/T_x}\,\mathrm{d}s
$$

---

#### 公式 (15)：Fourier 历史变量递推（核心创新，严格推导）

$$
\mathcal{H}_k(n)
= e^{i\pi k\Delta t/T_x}\,\mathcal{H}_k(n-1)
+ c_k\,U_{n-N_1}\int_{t_{n-N_1-1}}^{t_{n-N_1}}e^{i\pi k(t_n-s)/T_x}\,\mathrm{d}s
\tag{15}
$$

**推导**：

**第 1 步** — 将 $\mathcal{H}_k(n)$ 中的求和按"新进一项 + 其余旧项"拆分：

$$
\mathcal{H}_k(n) = c_k\,U_{n-N_1}\int_{t_{n-N_1-1}}^{t_{n-N_1}}e^{i\pi k(t_n-s)/T_x}\,\mathrm{d}s
+ c_k\sum_{j=1}^{n-1-N_1}U_j\int_{t_{j-1}}^{t_j}e^{i\pi k(t_n-s)/T_x}\,\mathrm{d}s
$$

**数学事实**：有限求和拆分，即 $\sum_{j=1}^{M}=(\text{第}\,M\text{项})+\sum_{j=1}^{M-1}$，其中 $M=n-N_1$。

**第 2 步** — 对旧项中的指数因子分解：

$$
e^{i\pi k(t_n-s)/T_x}
= e^{i\pi k(t_n-t_{n-1})/T_x}\cdot e^{i\pi k(t_{n-1}-s)/T_x}
= e^{i\pi k\Delta t/T_x}\cdot e^{i\pi k(t_{n-1}-s)/T_x}
$$

**数学事实**：指数函数加法公式 $e^{a+b}=e^a\cdot e^b$，以及 $t_n-t_{n-1}=\Delta t$。

**第 3 步** — 提出公因子：

$$
c_k\sum_{j=1}^{n-1-N_1}U_j\int_{t_{j-1}}^{t_j}e^{i\pi k(t_n-s)/T_x}\,\mathrm{d}s
= e^{i\pi k\Delta t/T_x}
\underbrace{c_k\sum_{j=1}^{(n-1)-N_1}U_j\int_{t_{j-1}}^{t_j}e^{i\pi k(t_{n-1}-s)/T_x}\,\mathrm{d}s}_{=\,\mathcal{H}_k(n-1)}
$$

**数学事实**：认出 $\mathcal{H}_k(n-1)$ 的定义式（时刻替换 $n\to n-1$）。

合并即得 (15)。

**关键意义**：$\mathcal{H}_k(n)$ 从 $\mathcal{H}_k(n-1)$ 只需 $O(1)$ 操作更新，与 $n$ 无关；$L+1$ 个模态共需 $O(L)$ 操作，彻底解除了远历史的线性增长代价。

---

#### 公式 (16)：完整步进方案（关键公式）

$$
U_n = d^{-1}f(t_n)
- d^{-1}\sum_{j=n-N_1+1}^{n-1}U_j\int_{t_{j-1}}^{t_j}\phi(t_n-s)\,\mathrm{d}s
- d^{-1}\sum_{k=0}^{L}\mathcal{H}_k(n)
\tag{16}
$$

**作用**：将近历史精确积分与 Fourier 远历史递推统一为单一显式更新公式，是实际时间推进的直接依据。

**时间推进算法（完整流程）**：

1. 用公式 (12) 计算 $U_1,U_2,\dots,U_{N_1}$（朴素矩形法预热）；
2. 初始化 $\mathcal{H}_k(N_1)=0$，$k=0,1,\dots,L$；
3. 对 $n=N_1+1,\dots,N_t$：
   a. 用 (15) 更新各 $\mathcal{H}_k(n)$（$O(L)$ 次运算）；
   b. 用 (16) 计算 $U_n$（近历史 $O(N_1)$ + Fourier 贡献 $O(L)$）。

**复杂度对比**：

| 方法 | 每步运算量 | 总运算量 | 存储量 |
|---|---|---|---|
| 朴素矩形（公式 (12)） | $O(n)$ | $O(N_t^2)$ | $O(N_t)$ |
| Fourier 代理（公式 (16)） | $O(N_1+L)$ | $O(N_t(N_1+L))$ | $O(N_1+L)$ |

当 $N_1=\xi N_t$（$\xi\ll 1$，如 $\xi=5\%$）且 $L\ll N_t$ 时，两者均大幅降低。

---

### 3.5 第 5 节：数值实验中的参数与误差关系式

**参数设置**：

$$
\phi_0=0.2,\quad \alpha=-0.5,\quad \bar{t}=0,\quad T=10
$$

$$
T_1\in\{0.5,\,0.1,\,0.05\},\quad
N_t\in\{2^n:n=5,\dots,16\},\quad
L\in\{2^{2n+1}:n=1,\dots,5\}
$$

**误差指标**：

$$
|u(T)-U_{N_t}|, \qquad
\text{相对误差} = \frac{|u(T)-U_{N_t}|}{\|u\|_{L^\infty(0,T)}}
$$

其中 $\|u\|_{L^\infty(0,T)}=\|t^{0.5}\|_{L^\infty(0,10)}=\sqrt{10}\approx 3.16$。

**实现细节**（对应代码中的预处理）：

$$
\Delta t = T/N_t,\quad
N_1 = \lceil T_1/\Delta t\rceil,\quad
T_1 \leftarrow N_1\Delta t,\quad
T_x = T + T_1
$$

**讨论中的优化关系式**（稀疏规则思想）：

$$
\Delta' t = T/N_1' \sim \sqrt{\Delta t}, \quad
N_1' = \sqrt{T_1 N_1}
$$

若 $N_1=\xi N_t$，则 $N_1' = \xi\sqrt{T N_t}$，可将近历史负担从 $O(N_1)$ 降至 $O(\sqrt{N_1})$。

---

## 四、论文流程串联总表

| 环节 | 该环节作用 | 该环节对应公式 | 公式作用 |
|---|---|---|---|
| 物理建模起点 | 把材料记忆效应写成历史卷积 | (1), (2) | 给出问题来源与两类核（Prony 指数核 vs 幂律核）的对比 |
| 介电模型类比 | 说明同类弱奇异卷积在电磁模型中的出现 | (4), (5), (6) | 拓宽应用背景，证明研究的普适性 |
| 分数阶联系建立 | 解释幂律核与分数积分的等价关系 | (3), (8) | 将问题纳入分数阶积分方程框架 |
| 统一求解对象 | 将问题抽象为标量第二类 Volterra 方程 | (7), (10) | 明确数值算法的最终求解方程 |
| 核函数泛化 | 引入 $\bar{t}$ 参数适配实验数据拟合 | (9) | 同时覆盖奇异与非奇异核，提升代码通用性 |
| 基准验证闭环 | 构造可计算精确解和强迫函数 | (11) | 为收敛阶验证提供解析参照，通过 Laplace + Euler 反射公式推导 |
| 朴素离散基线 | 建立乘积矩形精确权重历史求和 | (12) | 提供对比基线，揭示 $O(N_t^2)$ 计算与 $O(N_t)$ 存储代价 |
| 近远分解 | 将历史积分按 $T_1$ 拆成近历史（精确核）与远历史（代理核） | (13), (14) | 为引入 Fourier 代理创造合法分区结构 |
| Fourier 代理展开 | 将远历史代理核展开为有限 Fourier 级数 | (第 3 节 Fourier 系数公式) | 将连续代理函数变为有限个复指数模态之和 |
| 递推核心 | 将每个 Fourier 模态的历史变量 $\mathcal{H}_k(n)$ 改写为递推 | (15) | 把远历史每步代价从 $O(n)$ 降为 $O(1)$，是全文核心创新 |
| 完整算法闭合 | 合并对角分母、近历史精确积分和 Fourier 远历史递推 | (16) | 给出可直接编程实现的最终步进公式 |
| 数值评测 | 在多参数组合下验证误差与时间性能 | 第 5 节参数式、误差/时间图 | 证明方法可行性，揭示收敛瓶颈与改进方向 |

---

## 五、简要结论

1. **研究问题**：弱奇异幂律核 Volterra 积分方程的时间推进中 $O(N_t^2)$ 运算量与 $O(N_t)$ 存储量的工程瓶颈。

2. **核心创新**：将远历史卷积用 Fourier 级数代理，并将每个模态的历史积分改写为仅依赖上一步的递推公式 (15)，从而在不牺牲近场精度的条件下大幅降低远历史负担。

3. **代码对应**：  
   - 公式 (12) → `solver=1`（基准）；  
   - 公式 (14) → `solver=3` 分拆逻辑；  
   - 公式 (15) → `phistarR/phistarI` 递推循环；  
   - 公式 (16) → 每步最终 $U_n$ 更新语句。

4. **方法局限**：$T_1$ 固定，整体复杂度仍为 $O(N_t(N_1+L))$，并非完全消除历史负担，但在工程实际中已能将存储和运算量降低一到两个数量级，且对现有含 Prony 递推的商业代码改造成本极低。
