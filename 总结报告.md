# 总结报告

**论文**：*Approximate Fourier series recursion for problems involving temporal fractional calculus*  
**作者**：Simon Shaw, John R. Whiteman（Brunel University London，2022）  
**发表期刊**：*Computer Methods in Applied Mechanics and Engineering*，DOI: [10.1016/j.cma.2022.115537](https://doi.org/10.1016/j.cma.2022.115537)

---

## 第一部分：摘要概括

分数阶微积分本构模型在粘弹性聚合物、介电色散介质、生物组织等大量工程材料中有广泛应用，其核心数学结构是含**弱奇异幂律核**的卷积型 Volterra 积分方程。对该方程进行数值时间推进时，朴素正交积分法在每个时间步都需对整段历史积分重新求值，导致 \(O(N_t^2)\) 的运算量和 \(O(N_t)\) 的存储量（\(N_t\) 为时间步总数），严重制约大规模计算的可行性。本文提出一种新的高效方法：在距当前时刻较远的历史积分区段上，用一个光滑的 Fourier 级数代理函数 \(\psi\) 替换奇异核 \(\varphi\)，从而将远历史积分转化为一组可**递归更新**的复数历史变量 \(\mathcal{H}_k\)；弱奇异区段（近历史，长度为 \(T_1\)）仍用乘积矩形/梯形规则处理。代理函数通过 Hermite 多项式样条拼接实现 \(C^m\) 光滑性并做偶周期延拓，从而使 Fourier 系数快速衰减。该递归结构与商业代码中已普遍用于 Prony 指数核的更新方式完全兼容，具有极低的工程嵌入门槛。

---

## 第二部分：核心问题、关键公式与解决方案

### 2.1 待求解的核心问题

**问题**：在时间区间 \(I = [0, T]\) 上求实值函数 \(u(t)\)，使之满足 Volterra 第二类积分方程：

\[
u(t) + \int_{0}^{t} \varphi(t - s)\, u(s)\, ds = f(t) \tag{10}
\]

其中核函数取广义幂律形式：

\[
\varphi(t) = \varphi_0 \cdot (t + \bar{t})^{\alpha}, \quad \alpha \in (-1,\, 0),\quad \bar{t} \geq 0,\quad \varphi_0 > 0
\]

**符号说明**：

| 符号 | 含义 |
|------|------|
| \(u(t)\) | 待求解函数（如应力、极化强度等物理量） |
| \(\varphi(t)\) | 弱奇异幂律卷积核，\(\alpha \in (-1,0)\) 使 \(t=0\) 处奇异但可积 |
| \(f(t)\) | 已知强迫函数 |
| \(\bar{t} \geq 0\) | 奇异性偏移参数，\(\bar{t}=0\) 时退化为纯分数阶积分 |
| \(\varphi_0\) | 核函数振幅系数 |

**核心难点**：当 \(\bar{t}=0\) 时，\(\varphi(t) = \varphi_0 t^\alpha\) 在 \(t=0\) 处弱奇异，与 Riemann-Liouville 分数阶积分算子直接关联：

\[
u(t) + \varphi_0\,\Gamma(1+\alpha)\, D^{-\alpha-1} u(t) = f(t) \tag{8}
\]

**数值计算困境**：离散时间步 \(\Delta t = T/N_t\) 后，朴素乘积矩形积分给出：

\[
U_n + \sum_{j=1}^{n} U_j \int_{t_{j-1}}^{t_j} \varphi(t_n - s)\, ds = f(t_n)
\]

每步需对 \(j=1,\ldots,n-1\) 求和，总运算量为 \(O(N_t^2)\)，需存储所有历史值 \(U_1,\ldots,U_{n-1}\)，内存 \(O(N_t)\)。在大规模 PDE 求解中这是不可接受的。

---

### 2.2 核心公式逐一解读

#### 公式 (11)：验证用精确解与强迫函数

取精确解 \(u(t) = t^{-\alpha}\)（注意 \(-\alpha \in (0,1)\)），当 \(\bar{t}=0\) 时强迫函数闭合表达式为：

\[
f(t) = t^{-\alpha} + \varphi_0\, t^{\alpha}\, \frac{\pi}{\sin(\alpha\pi)} \tag{11}
\]

**推导**：利用卷积的 Laplace 变换公式，\(t^\alpha * t^{-\alpha} = t \cdot \Gamma(1-\alpha)\Gamma(1+\alpha) = \alpha\pi/\sin(\alpha\pi) \cdot t^0 \cdot t\)，再利用 \(\Gamma(1-\alpha)\Gamma(1+\alpha) = \alpha\pi/\sin(\alpha\pi)\) 即得。数值实验中取 \(\varphi_0=0.2\)、\(\alpha=-0.5\)、\(T=10\) 验证收敛率。

---

#### 公式 (12)：乘积矩形规则（基准方法）

对 \(n=1,2,\ldots\) 逐步求解：

\[
U_n = \frac{f(t_n) - \displaystyle\sum_{j=1}^{n-1} \frac{\varphi_0\, U_j}{\alpha+1} \left[(\bar{t}+(n-j+1)\Delta t)^{\alpha+1} - (\bar{t}+(n-j)\Delta t)^{\alpha+1}\right]}{d} \tag{12}
\]

其中分母（对角项）：

\[
d = 1 + \frac{\varphi_0}{\alpha+1}\left[(\bar{t}+\Delta t)^{\alpha+1} - \bar{t}^{\alpha+1}\right]
\]

**解读**：每段 \([t_{j-1}, t_j]\) 上将 \(u(s)\approx U_j\)（矩形近似），核 \(\varphi\) 精确积分得到权重系数 \(\varphi_0/(\alpha+1)[...]^{\alpha+1}\)。分母 \(d\) 对应 \(j=n\) 的对角项（不含历史）。此公式构成**朴素 \(O(N_t^2)\) 基准法**。

**对应代码**（`fouvol.py`，solver=1）：

```python
# 分母（对角项，与步骤无关，仅计算一次）
denom = 1.0 + varphi0/(alpha+1)*( dt**(alpha+1) )   # tbar=0 情形

for i in range(1, Nt+1):
    # 历史求和：j=1,...,i-1
    hist = sum([
        varphi0/(alpha+1) * (
            ((i-j+1)*dt + tbar)**(alpha+1) - ((i-j)*dt + tbar)**(alpha+1)
        ) * uvals[j]
        for j in range(1, i)
    ])
    uvals[i] = ( ft(i*dt, varphi0, alpha) - hist ) / denom
```

---

#### 公式 (14)：历史积分的近/远分拆（Fourier 代理引入）

选定 \(T_1 \in (0,T)\)，令 \(N_1 = T_1/\Delta t\)（整数），将历史积分拆成**近历史**（近 \(N_1\) 步，用精确积分处理奇异性）和**远历史**（超过 \(N_1\) 步，用 Fourier 代理 \(\psi\) 替换 \(\varphi\)）：

\[
\left(1 + \int_{t_{n-1}}^{t_n} \varphi(t_n-s)\,ds\right) U_n
= f(t_n)
- \underbrace{\sum_{j=n-N_1+1}^{n-1} U_j \int_{t_{j-1}}^{t_j} \varphi(t_n-s)\,ds}_{\text{近历史（精确积分）}}
- \underbrace{\sum_{k=0}^{L} c_k \sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_n-s)/T_x}\,ds}_{\text{远历史（Fourier代理）}}
\tag{14}
\]

其中 Fourier 代理函数 \(\psi\) 满足 \(\psi\big|_{[T_1,T]} = \varphi\big|_{[T_1,T]}\)，其 Fourier 系数为：

\[
c_k = \begin{cases} \dfrac{2}{T_x}\displaystyle\int_0^{T_x} \psi(t)\cos\!\left(\dfrac{\pi k t}{T_x}\right)dt, & k \neq 0 \\ \dfrac{1}{T_x}\displaystyle\int_0^{T_x} \psi(t)\, dt, & k = 0 \end{cases}
\]

**解读**：近历史 \([t_{n-N_1+1}, t_{n-1}]\) 内核 \(\varphi\) 可能仍奇异（若 \(t_n - t_{n-N_1+1} < T_1\)），故沿用精确乘积积分；远历史 \([t_1, t_{n-N_1}]\) 上时间差 \(t_n - s > T_1\)，核远离奇异点，可用光滑代理 \(\psi\) 替换，进而用 Fourier 级数展开以实现递归。

---

#### 公式 (15)：Fourier 历史变量的递归更新（核心创新）

定义复数历史积分量：

\[
\mathcal{H}_k(n) := c_k \sum_{j=1}^{n-N_1} U_j \int_{t_{j-1}}^{t_j} e^{i\pi k(t_n-s)/T_x}\,ds
\]

则 \(\mathcal{H}_k(n)\) 满足递推关系：

\[
\boxed{
\mathcal{H}_k(n) = e^{i\pi k \Delta t/T_x}\, \mathcal{H}_k(n-1)
+ c_k\, U_{n-N_1} \int_{t_{n-N_1-1}}^{t_{n-N_1}} e^{i\pi k(t_n-s)/T_x}\,ds
}
\tag{15}
\]

**公式解读**：

- **第一项** \(e^{i\pi k\Delta t/T_x}\,\mathcal{H}_k(n-1)\)：将前一步的历史积分量按复数旋转因子 \(\omega_k = e^{i\pi k\Delta t/T_x}\) 向前推进一个时间步（如同复平面上旋转 \(\Delta\theta = \pi k\Delta t/T_x\)）。
- **第二项**：加入新纳入远历史范围的那一步 \(U_{n-N_1}\) 的贡献（矩形积分精确计算）。
- **关键性质**：更新只需 \(O(L)\) 次运算，与 \(n\) 无关，彻底摆脱 \(O(N_t)\) 的历史依赖。

**与 Prony 指数核递归的类比**：若 \(\varphi(t) = \sum_q \varphi_q e^{-t/\tau_q}\)，则历史积分也满足类似递推 \(\mathcal{H}_q(n) = e^{-\Delta t/\tau_q}\mathcal{H}_q(n-1) + \varphi_q U_{n-1}\Delta t\)，本文方法正是将此思路推广到弱奇异核。

**对应代码**（`fouvol.py`，solver=3，\(j=-L,\ldots,-1\) 对应负频率分量）：

```python
for j in range(-Nlim, 0):            # j = -L, ..., -1  （利用实偶性, 正频分量通过共轭合并）
    Rpart = phistarR[j+Nlim]         # Re[H_j(n-1)]
    Ipart = phistarI[j+Nlim]         # Im[H_j(n-1)]
    costerm = math.cos(math.pi*j*dt/Tx)   # Re[e^{iπjΔt/Tx}]
    sinterm = math.sin(math.pi*j*dt/Tx)   # Im[e^{iπjΔt/Tx}]
    # 复数乘法：ω_j * H_j(n-1) —— 即公式(15)第一项
    phistarR[j+Nlim] = costerm*Rpart - sinterm*Ipart
    phistarI[j+Nlim] = sinterm*Rpart + costerm*Ipart
    # 加入新步 U_{n-N1} 的贡献 —— 即公式(15)第二项
    cosT1 = math.cos(math.pi*j*T1/Tx)
    sinT1 = math.sin(math.pi*j*T1/Tx)
    coeff = cnR[j+Nlim] * uvals[i-N1] * Tx / j / math.pi
    phistarR[j+Nlim] += coeff * (cosT1*sinterm - sinT1*(1.0 - costerm))
    phistarI[j+Nlim] += coeff * (cosT1*(1.0 - costerm) + sinT1*sinterm)

# j=0 的特殊情形（无相位旋转）
phistarR[0+Nlim] += cnR[0+Nlim] * uvals[i-N1] * dt
```

---

#### 公式 (16)：完整时间推进方案（近历史 + 远历史 Fourier 递归）

\[
U_n = d^{-1} f(t_n)
      - d^{-1} \sum_{j=n-N_1+1}^{n-1} \frac{\varphi_0\, U_j}{\alpha+1}
        \left[(\bar{t}+(n-j+1)\Delta t)^{\alpha+1} - (\bar{t}+(n-j)\Delta t)^{\alpha+1}\right]
      - d^{-1} \sum_{k=0}^{L} \mathcal{H}_k(n)
\tag{16}
\]

**时间推进算法**：

1. 用公式 (12)（朴素矩形法）计算 \(U_1, U_2, \ldots, U_{N_1}\)；
2. 初始化 \(\mathcal{H}_k(N_1) = 0\)，\(k = 0, 1, \ldots, L\)；
3. 对 \(n = N_1+1, N_1+2, \ldots, N_t\)：
   a. 用公式 (15) 更新 \(\mathcal{H}_k(n)\)（\(O(L)\) 次运算）；
   b. 用公式 (16) 求解 \(U_n\)（近历史 \(O(N_1)\) 次运算 + Fourier 贡献 \(O(L)\) 次运算）。

**存储与运算复杂度对比**：

| 方法 | 每步运算量 | 总运算量 | 存储量 |
|------|-----------|---------|--------|
| 朴素矩形法（公式12） | \(O(n)\) | \(O(N_t^2)\) | \(O(N_t)\) |
| Fourier 代理法（公式16） | \(O(N_1 + L)\) | \(O(N_t(N_1+L))\) | \(O(N_1 + L)\) |

当 \(N_1 = \xi N_t\)（\(\xi \ll 1\)，如 \(\xi=5\%\)）且 \(L \ll N_t\) 时，计算开销和存储量均大幅降低。

**对应代码**（`fouvol.py`，solver=3）：

```python
for i in range(1, Nt+1):
    # 1. 更新 Fourier 历史变量（见公式15，仅当 i > N1 时触发）
    if i > N1:
        # ... 见上方公式(15)对应代码 ...

    # 2. 近历史精确积分（矩形规则，jlo = max(1, i+1-N1)）
    jlo = int(max(1, i + 1 - N1))
    hist = varphi0/(alpha+1) * sum([
        (((i-j+1)*dt + tbar)**(alpha+1) - ((i-j)*dt + tbar)**(alpha+1)) * uvals[j]
        for j in range(jlo, i)
    ])

    # 3. 加入 Fourier 远历史贡献（见公式16第三项）
    if i > N1:
        hist += phistarR[0+Nlim]                         # k=0 项
        hist += 2 * sum([phistarR[j+Nlim] for j in range(-Nlim, 0)])  # k≠0 项（利用实偶性×2）

    # 4. 求解当前步
    uvals[i] = ( ft(i*dt, varphi0, alpha) - hist ) / denom
```

---

### 2.3 Fourier 代理函数 \(\psi\) 的构造

为使 Fourier 系数快速衰减（保证 \(L\) 较小时精度良好），\(\psi\) 需足够光滑。构造步骤：

1. **左侧 Hermite 多项式** \(p_L : [0, T_1] \to \mathbb{R}\)，次数 \(2m+1\)，满足：
   - \(p_L^{(k)}(0) = 0\)，\(k=1,\ldots,m\)（奇异点处零化导数，保证周期延拓光滑）；
   - \(p_L^{(k)}(T_1) = \varphi^{(k)}(T_1)\)，\(k=0,1,\ldots,m\)（与 \(\varphi\) 在 \(T_1\) 处 \(C^m\) 拼接）；

2. **右侧 Hermite 多项式** \(p_R : [T, T_x] \to \mathbb{R}\)（\(T_x = T + T_1\)），次数 \(2m+1\)，满足：
   - \(p_R^{(k)}(T) = \varphi^{(k)}(T)\)，\(k=0,\ldots,m\)（与 \(\varphi\) 在 \(T\) 处 \(C^m\) 拼接）；
   - \(p_R^{(k)}(T_x) = 0\)，\(k=1,\ldots,m\)（端点零化，保证周期延拓光滑）；

3. 拼接为 \([0,T_x]\) 上的函数：\(\psi = p_L\) on \([0,T_1]\)，\(\psi = \varphi\) on \([T_1,T]\)，\(\psi = p_R\) on \([T,T_x]\)；

4. 对 \(t=0\) 做**偶对称延拓**至 \([-T_x, T_x]\)，再做 \(2T_x\) 周期延拓至全实轴。

光滑阶次 \(m\) 越高，\(\psi \in C^m\)，Fourier 系数以 \(O(k^{-(m+1)})\) 速率衰减，同等精度所需 \(L\) 越小。

**对应代码**（`fouker.py`，函数 `get_FourierCoefficientsNew`）：

```python
# 左侧Hermite插值节点（含重节点，体现导数约束）
# 右侧Hermite插值节点
# 用 get_DDHermite 构造除差表，get_DDHermiteValue 用Horner法求值
# 积分 ∫ψ(t)cos(πkt/Tx) dt 用 scipy.integrate.quad 数值计算
```

---

## 第三部分：文章的创新点

### 3.1 以 Fourier 级数为弱奇异核的"代理"——解决 O(N²) 计算瓶颈

**现有方法的问题**：文献中已有快速卷积法（Schädle et al. [18]，McLean [19]）等，但它们通常需要大幅改变时间积分算法的架构。

**本文创新**：只需对 \([T_1, T]\) 区段上的弱奇异核做一次性替换，无需改变近历史段的积分结构。核心观察是：当 \(t - s > T_1\) 时，弱奇异核 \(\varphi(t-s)\) 已远离奇异点，行为光滑，可以被 Fourier 级数高精度逼近，且 Fourier 级数的指数型基函数天然支持递归更新。

### 3.2 Hermite 样条保证 \(C^m\) 光滑偶周期延拓——Fourier 系数的高阶衰减

**现有方法的问题**：直接对 \(\varphi\) 做周期延拓会在端点产生跳跃，导致 Fourier 系数仅以 \(O(k^{-1})\) 衰减（Gibbs 现象），需要非常大的 \(L\) 才能精确。

**本文创新**：在 \([0, T_1]\) 和 \([T, T_x]\) 两端分别构造**奇异性感知的 Hermite 多项式**：左端强制各阶导数为零以平滑奇异点，右端以 \(C^m\) 方式过渡到零，使 \(\psi \in C^m(\mathbb{R})\)，Fourier 系数衰减率提升至 \(O(k^{-(m+1)})\)。增大 \(m\) 可在使用较小 \(L\) 的前提下保持精度。这是一种**精度与计算量的联合优化**策略。

### 3.3 与现有商业代码无缝对接——极低工程嵌入门槛

**现有商业代码现状**：粘弹性 FEM 商业软件（如 Abaqus 等）对 Prony 指数型松弛函数已普遍采用递归更新公式：

\[
\mathcal{H}_q(n) = e^{-\Delta t/\tau_q}\,\mathcal{H}_q(n-1) + \varphi_q\, U_{n-1}\,\Delta t
\]

**本文创新**：公式 (15) 与上式的结构**完全同构**——只需将衰减因子 \(e^{-\Delta t/\tau_q}\) 替换为复数旋转因子 \(e^{i\pi k\Delta t/T_x}\)，即可在现有 Prony 递归框架内插入 Fourier 代理。论文作者将此定性为"对弱奇异核进行微创手术"（*minor surgery on the weakly singular kernel*），使分数阶本构模型进入商业代码的障碍大幅降低。

### 3.4 可变时间步长扩展性

**本文创新**：公式 (15) 的推导过程对 \(\Delta t\) 无固定步长要求，仅需将单步贡献积分中的 \(\Delta t\) 替换为当前步长 \(\Delta t_n\) 即可。这使得算法天然适配**自适应时间步长**策略（如 [16, 17] 中所述），为未来与自适应 FEM 时间推进方案结合提供了理论基础。

---

## 附录：核心符号对照表

| 符号 | 代码变量 | 含义 |
|------|----------|------|
| \(\varphi(t)\) | `varphi0 * (t+tbar)**alpha` | 弱奇异幂律核 |
| \(\alpha\) | `alpha` | 奇异指数，\(\alpha \in (-1,0)\) |
| \(\varphi_0\) | `varphi0` | 核振幅系数（默认0.2） |
| \(\bar{t}\) | `tbar` | 奇异偏移（默认0） |
| \(T\) | `T` | 时间区间终点 |
| \(T_1\) | `T1` | 近/远历史分界点 |
| \(T_x = T + T_1\) | `Tx` | 周期延拓半周期 |
| \(N_t\) | `Nt` | 时间步总数 |
| \(\Delta t\) | `dt` | 时间步长 |
| \(N_1 = T_1/\Delta t\) | `N1` | 近历史步数 |
| \(L\) | `Nlim` | Fourier 级数截断阶数 |
| \(m\) | `smoothness` | Hermite 插值光滑阶数 |
| \(c_k\) | `cnR[k+Nlim]` | Fourier 系数（实数，因 \(\psi\) 为偶函数） |
| \(\mathcal{H}_k(n)\) | `phistarR[k+Nlim] + i*phistarI[k+Nlim]` | 第 \(k\) 阶 Fourier 历史变量 |
| \(U_n\) | `uvals[n]` | \(u(t_n)\) 的数值近似 |
| \(d\) | `denom` | 对角项分母 |
| \(\psi\) | 由 `p_L`、\(\varphi\)、`p_R` 拼接 | Fourier 代理函数（光滑偶周期） |
| \(f(t)\) | `ft(t,varphi0,alpha)` | 强迫函数（对应精确解 \(u=t^{-\alpha}\)） |
| \(u(t)=t^{-\alpha}\) | `ut(t,alpha)` | 数值验证用精确解 |
